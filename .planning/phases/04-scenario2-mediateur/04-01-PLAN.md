---
phase: 04-scenario2-mediateur
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - formation_causalite.ipynb
autonomous: true
requirements: [SC2-01, SC2-02, SC2-03]

must_haves:
  truths:
    - "Le scénario 2 affiche un DAG 4 noeuds (Pub, Nb visites, Panier moyen, Ventes) en diamond layout, sans arête directe Pub→Ventes, exporté figures/sc2_dag.png"
    - "Le coefficient plot montre OLS sans médiateur (~30.8%) > OLS avec panier_moyen (~21.1%) — biais vers le bas visible et quantifié"
    - "Le bar chart compare 3 estimateurs : OLS sans médiateur (rouge, correct), OLS avec médiateur (bleu, biaisé), Valeur vraie ATT (vert)"
    - "L'ATT contrefactuel est calculé avec rng_cf_sc2 = np.random.default_rng(SEED+41), isolé du rng_sc2 de génération"
    - "CSV exporté data/sc2_mediateur.csv"
    - "Le notebook s'exécute de bout en bout (nbconvert exit 0) avec 36 cellules au total"
  artifacts:
    - path: "figures/sc2_dag.png"
      provides: "DAG Scénario 2 — deux chemins médiateurs"
    - path: "figures/sc2_coeff.png"
      provides: "Coefficient plot Scénario 2"
    - path: "figures/sc2_bar.png"
      provides: "Bar chart Scénario 2"
    - path: "data/sc2_mediateur.csv"
      provides: "Dataset scénario 2"
  key_links:
    - from: "code-sc2-data"
      to: "df_sc2['pub']"
      via: "assignation aléatoire niveau magasin (p=0.5, drop_duplicates)"
      pattern: "rng_sc2\\.binomial\\(1, 0\\.5"
    - from: "code-sc2-data"
      to: "model_naive_sc2"
      via: "OLS log_rev_int ~ pub (sans médiateur = effet total correct)"
      pattern: "log_rev_int ~ pub'"
    - from: "code-sc2-data"
      to: "model_med_sc2"
      via: "OLS log_rev_int ~ pub + panier_moyen (bloque chemin médiateur = biaisé)"
      pattern: "log_rev_int ~ pub \\+ panier_moyen"
    - from: "code-sc2-coeff"
      to: "att_sc2_log"
      via: "valeur vraie = contrefactuel (log_Y1 - log_Y0).mean()"
      pattern: "att_sc2_log"
---

<objective>
Insérer le scénario 2 (surcontrôle sur un médiateur) dans le notebook : 5 cellules (1 markdown + 4 code), exécuter le notebook complet, exporter 3 PNG et 1 CSV.

Purpose: Illustrer que contrôler sur `panier_moyen` (un médiateur) dans la régression biaise le coefficient de `pub` vers le bas, même quand l'assignation est parfaitement aléatoire. Contraste pédagogique avec Phase 3 : ici le naïf est correct, et le "sophistiqué" est biaisé.

Output: 3 PNG (sc2_dag.png, sc2_coeff.png, sc2_bar.png), 1 CSV (sc2_mediateur.csv), 5 nouvelles cellules dans le notebook (cellules 31-35, soit 36 cellules au total).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-scenario2-mediateur/04-CONTEXT.md
@.planning/phases/04-scenario2-mediateur/04-RESEARCH.md

<interfaces>
<!-- Fonctions DGP disponibles dans le notebook (cellules 4-5, déjà définies) -->

```python
# generate_base_panel(params, rng) — retourne base_df (4800 lignes, 200 magasins × 24 mois)
# Colonnes : magasin_id, taille, urbain, qualite_equipe, mois, effet_saison_val,
#            n_potentiel, nb_visites, ventes, panier_moyen (baseline pub=0)

# compute_outcomes(df, params, rng) — recalcule ventes et panier_moyen selon df['pub']
# Fait .copy() en interne — safe à appeler plusieurs fois

# smf = statsmodels.formula.api (importé cellule 2)
# nx = networkx (importé cellule 2)
# base_df = panel partagé disponible en mémoire (cellule 5 déjà exécutée)
# SEED = 42 (cellule 1)
# PARAMS dict disponible (cellule 1)
```

<!-- Pattern OLS avec DV log-points (Phase 3 refactoring validé) -->

```python
df['log_rev_int'] = np.log(df['ventes'] / df['n_potentiel'])
model = smf.ols('log_rev_int ~ pub', data=df).fit()
coef = model.params['pub']          # float, en log-points ≈ %
ci   = model.conf_int()             # colonnes 0 et 1 (integers)
lower = ci.loc['pub', 0]
upper = ci.loc['pub', 1]
xerr = [[coef - lower], [upper - coef]]   # distances positives pour errorbar
```

<!-- Pattern assignation pub niveau magasin (sc1a/sc1b) -->

```python
rng_sc2 = np.random.default_rng(SEED + 40)
stores_sc2 = base_df[['magasin_id']].drop_duplicates('magasin_id').copy()
stores_sc2['pub'] = rng_sc2.binomial(1, 0.5, size=len(stores_sc2))
df_sc2 = base_df.merge(stores_sc2[['magasin_id', 'pub']], on='magasin_id')
df_sc2 = compute_outcomes(df_sc2, PARAMS, rng_sc2)
```

<!-- Pattern ATT contrefactuel (sc1a pattern) -->

```python
rng_cf_sc2 = np.random.default_rng(SEED + 41)   # seed dédié, isolé
df_cf_sc2 = df_treated_sc2.copy()
df_cf_sc2['pub'] = 0
df_cf_sc2 = compute_outcomes(df_cf_sc2, PARAMS, rng_cf_sc2)
att_sc2_log = (log_Y1_sc2 - log_Y0_sc2).mean()
```

<!-- Résultats empiriques attendus (SEED=42) -->

```
ATT_sc2_log     ≈ 30.0%   (valeur vraie contrefactuelle)
OLS sans méd    ≈ 30.8%   (correct — unbiased avec assignation aléatoire)
OLS avec méd    ≈ 21.1%   (biaisé vers le bas ~9pp — contrôle bloque chemin panier)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insérer les 5 cellules du scénario 2 et exécuter le notebook</name>
  <files>formation_causalite.ipynb</files>
  <action>
Insérer 5 cellules après la cellule index 30 (dernière cellule actuelle = code-sc1c-bar). Le notebook passe de 31 à 36 cellules.

**Cellule 1 — Markdown section sc2 (cell_type: markdown) :**
```
---
## Scénario 2 — Surcontrôle sur un médiateur

`panier_moyen` est un médiateur : `pub → panier_moyen → ventes`. Contrôler sur le médiateur dans la régression bloque mécaniquement ce chemin causal et biaise le coefficient de `pub` vers le bas — même si l'assignation est parfaitement aléatoire. Le modèle naïf (sans contrôle) capture l'effet total et est ici correct.
```

**Cellule 2 — code-sc2-data (cell_type: code) :**
```python
# ─────────────────────────────────────────────
# Scénario 2 — Surcontrôle sur un médiateur
# Assignation pub : aléatoire niveau magasin (pas de confondant)
# Médiateur bloqué : panier_moyen
# ─────────────────────────────────────────────

rng_sc2 = np.random.default_rng(SEED + 40)

# Assignation pub aléatoire au niveau magasin (p=0.5 — pas de confondant)
stores_sc2 = base_df[['magasin_id']].drop_duplicates('magasin_id').copy()
stores_sc2['pub'] = rng_sc2.binomial(1, 0.5, size=len(stores_sc2))

# Panel complet + outcomes observés
df_sc2 = base_df.merge(stores_sc2[['magasin_id', 'pub']], on='magasin_id')
df_sc2 = compute_outcomes(df_sc2, PARAMS, rng_sc2)
df_sc2['log_rev_int'] = np.log(df_sc2['ventes'] / df_sc2['n_potentiel'])

# Calcul ATT contrefactuel (rng dédié pour isolation)
treated_ids_sc2 = stores_sc2[stores_sc2['pub'] == 1]['magasin_id'].values
df_treated_sc2 = df_sc2[df_sc2['magasin_id'].isin(treated_ids_sc2)].copy()
rng_cf_sc2 = np.random.default_rng(SEED + 41)
df_cf_sc2 = df_treated_sc2.copy()
df_cf_sc2['pub'] = 0
df_cf_sc2 = compute_outcomes(df_cf_sc2, PARAMS, rng_cf_sc2)
df_cf_sc2['log_rev_int'] = np.log(df_cf_sc2['ventes'] / df_cf_sc2['n_potentiel'])
log_Y1_sc2 = np.log(df_treated_sc2['ventes'] / df_treated_sc2['n_potentiel'])
log_Y0_sc2 = np.log(df_cf_sc2['ventes'] / df_cf_sc2['n_potentiel'])
att_sc2_log = (log_Y1_sc2 - log_Y0_sc2).mean()

# OLS sans médiateur : effet total (correct car assignation aléatoire)
model_naive_sc2 = smf.ols('log_rev_int ~ pub', data=df_sc2).fit()
# OLS avec médiateur : effet direct seulement (biaisé vers le bas)
model_med_sc2   = smf.ols('log_rev_int ~ pub + panier_moyen', data=df_sc2).fit()

# Export CSV
df_sc2.to_csv('data/sc2_mediateur.csv', index=False)
print(f"Sc2 — Traités: {len(treated_ids_sc2)}/200 magasins")
print(f"Sc2 — ATT: {att_sc2_log*100:.1f}%  |  OLS sans médiateur: {model_naive_sc2.params['pub']*100:.1f}%  |  OLS avec panier_moyen: {model_med_sc2.params['pub']*100:.1f}%")
```

**Cellule 3 — code-sc2-dag (cell_type: code) :**
```python
# ─────────────────────────────────────────────
# Figure DAG — Scénario 2
# Deux chemins causaux : Pub→Nb visites→Ventes ET Pub→Panier moyen→Ventes
# PAS d'arête directe Pub→Ventes (le DGP n'en a pas)
# ─────────────────────────────────────────────

G_sc2 = nx.DiGraph()
G_sc2.add_edges_from([
    ('Pub', 'Nb visites'),
    ('Pub', 'Panier moyen'),
    ('Nb visites', 'Ventes'),
    ('Panier moyen', 'Ventes'),
])
pos_sc2 = {
    'Pub': (0, 0.5),
    'Nb visites': (1, 1),
    'Panier moyen': (1, 0),
    'Ventes': (2, 0.5),
}
color_map_sc2 = {
    'Pub': 'steelblue',
    'Ventes': 'seagreen',
    'Nb visites': 'darkorange',
    'Panier moyen': 'mediumpurple',
}
node_colors_sc2 = [color_map_sc2[n] for n in G_sc2.nodes()]

fig, ax = plt.subplots(figsize=(6, 3.5))
nx.draw_networkx(G_sc2, pos_sc2, ax=ax,
                 node_color=node_colors_sc2,
                 node_size=2000, font_size=9, font_color='white',
                 arrows=True, arrowsize=20)
ax.axis('off')
ax.set_title('DAG — Scénario 2 : deux chemins causaux via médiateurs')
fig.savefig('figures/sc2_dag.png', dpi=150, bbox_inches='tight')
plt.show()
```

**Cellule 4 — code-sc2-coeff (cell_type: code) :**
```python
# ─────────────────────────────────────────────
# Figure coefficient plot — Scénario 2
# 3 points : OLS sans médiateur (correct), OLS avec panier_moyen (biaisé), ATT (vrai)
# ─────────────────────────────────────────────

coef_naive_sc2 = model_naive_sc2.params['pub']
ci_naive_sc2   = model_naive_sc2.conf_int()
xerr_naive_sc2 = [[coef_naive_sc2 - ci_naive_sc2.loc['pub', 0]],
                   [ci_naive_sc2.loc['pub', 1] - coef_naive_sc2]]

coef_med_sc2 = model_med_sc2.params['pub']
ci_med_sc2   = model_med_sc2.conf_int()
xerr_med_sc2 = [[coef_med_sc2 - ci_med_sc2.loc['pub', 0]],
                 [ci_med_sc2.loc['pub', 1] - coef_med_sc2]]

estimators_sc2 = ['OLS sans médiateur', 'OLS avec panier_moyen', 'Valeur vraie (ATT)']
y_pos_sc2 = [2, 1, 0]

fig, ax = plt.subplots(figsize=(8, 4))
ax.errorbar(coef_naive_sc2, 2, xerr=xerr_naive_sc2, fmt='o', capsize=5,
            color='#e74c3c', label='OLS sans médiateur', markersize=8)
ax.errorbar(coef_med_sc2, 1, xerr=xerr_med_sc2, fmt='o', capsize=5,
            color='#3498db', label='OLS avec panier_moyen', markersize=8)
ax.errorbar(att_sc2_log, 0, fmt='D', color='#2ecc71',
            label='Valeur vraie (ATT)', markersize=10)
ax.axvline(x=att_sc2_log, color='gray', linestyle='--', alpha=0.5)
ax.set_yticks(y_pos_sc2)
ax.set_yticklabels(estimators_sc2)
ax.set_xlabel('Uplift log des ventes (≈ %)')
ax.set_title('Scénario 2 — Coefficients OLS sans vs avec médiateur vs valeur vraie')
ax.legend(loc='lower right')
fig.savefig('figures/sc2_coeff.png', dpi=150, bbox_inches='tight')
plt.show()
```

**Cellule 5 — code-sc2-bar (cell_type: code) :**
```python
# ─────────────────────────────────────────────
# Figure bar chart — Scénario 2
# Sans médiateur (rouge, correct) vs Avec médiateur (bleu, biaisé) vs ATT (vert)
# ─────────────────────────────────────────────

fig, ax = plt.subplots(figsize=(7, 4))
labels_sc2 = ['OLS sans médiateur', 'OLS avec panier_moyen', 'Valeur vraie (ATT)']
values_sc2 = [coef_naive_sc2, coef_med_sc2, att_sc2_log]
colors_sc2 = ['#e74c3c', '#3498db', '#2ecc71']

ax.bar(range(len(labels_sc2)), values_sc2, color=colors_sc2, width=0.5)
ax.set_xticks(range(len(labels_sc2)))
ax.set_xticklabels(labels_sc2)
ax.set_ylabel('Uplift log des ventes (≈ %)')
ax.set_title('Scénario 2 — Comparaison des estimateurs')
ax.axhline(y=att_sc2_log, color='gray', linestyle='--', alpha=0.5)
fig.savefig('figures/sc2_bar.png', dpi=150, bbox_inches='tight')
plt.show()
```

Après insertion des cellules, exécuter le notebook complet :
```bash
jupyter nbconvert --to notebook --execute formation_causalite.ipynb \
    --output formation_causalite.ipynb \
    --ExecutePreprocessor.timeout=300
```

Anti-patterns CRITIQUES pour sc2 :
- PAS d'arête `('Pub', 'Ventes')` dans le DAG — sc2 n'a pas de chemin direct pub→ventes
- NE PAS appeler le modèle avec médiateur "OLS ajusté" — c'est le modèle BIAISÉ, l'appeler `model_med_sc2` et le labeler `'OLS avec panier_moyen'`
- PAS de `C(mois)` dans les formules OLS — assignation aléatoire, la saison n'est pas un confondant
- `rng_cf_sc2 = np.random.default_rng(SEED + 41)` — seed DÉDIÉ pour le contrefactuel (pas rng_sc2)
- `drop_duplicates('magasin_id')` OBLIGATOIRE avant l'assignation niveau magasin
- `node_colors_sc2 = [color_map_sc2[n] for n in G_sc2.nodes()]` — pas une liste littérale
- `fig.savefig(...)` AVANT `plt.show()` dans chaque cellule
- `coef_naive_sc2` doit être SUPÉRIEUR à `coef_med_sc2` — si c'est l'inverse, vérifier le DGP
  </action>
  <verify>
    <automated>python3 -c "
import json, os, subprocess

# 1. Vérifier structure des cellules sc2 dans le notebook
with open('formation_causalite.ipynb') as f:
    nb = json.load(f)
sources = [''.join(c['source']) for c in nb['cells']]
all_src = '\n'.join(sources)

checks = {
    'sc2 markdown present': 'Scénario 2' in all_src and 'médiateur' in all_src,
    'rng_sc2 SEED+40': 'np.random.default_rng(SEED + 40)' in all_src,
    'rng_cf_sc2 SEED+41': 'np.random.default_rng(SEED + 41)' in all_src,
    'pub assignment p=0.5': 'binomial(1, 0.5' in all_src,
    'log_rev_int sc2': 'df_sc2[\"log_rev_int\"]' in all_src or \"df_sc2['log_rev_int']\" in all_src,
    'OLS sans mediateur': \"'log_rev_int ~ pub'\" in all_src or '\"log_rev_int ~ pub\"' in all_src,
    'OLS avec panier_moyen': 'log_rev_int ~ pub + panier_moyen' in all_src,
    'att_sc2_log computed': 'att_sc2_log' in all_src,
    'DAG 4 nodes no direct': 'G_sc2' in all_src and 'Nb visites' in all_src and 'Panier moyen' in all_src,
    'no direct Pub Ventes edge': \"('Pub', 'Ventes')\" not in all_src or 'G_sc2' not in all_src.split(\"('Pub', 'Ventes')\")[0][-200:],
    'sc2_dag.png exported': 'sc2_dag.png' in all_src,
    'sc2_coeff.png exported': 'sc2_coeff.png' in all_src,
    'sc2_bar.png exported': 'sc2_bar.png' in all_src,
    'sc2_mediateur.csv exported': 'sc2_mediateur.csv' in all_src,
    'xlabel uplift log': 'Uplift log des ventes' in all_src,
    'nb >= 36 cells': len(nb['cells']) >= 36,
}
for k, v in checks.items():
    print(f'{\"OK\" if v else \"FAIL\"}: {k}')
print(f'Total cells: {len(nb[\"cells\"])}')

# 2. Vérifier les fichiers exportés
files_to_check = [
    'figures/sc2_dag.png',
    'figures/sc2_coeff.png',
    'figures/sc2_bar.png',
    'data/sc2_mediateur.csv',
]
for p in files_to_check:
    exists = os.path.exists(p)
    checks[p] = exists
    print(f'{\"OK\" if exists else \"FAIL\"}: {p} exists')

# 3. Résultat global
failed = [k for k, v in checks.items() if not v]
if failed:
    print(f'FAIL: {len(failed)} checks failed: {failed}')
    exit(1)
print('PASS: all checks passed')
"
</automated>
  </verify>
  <done>5 cellules scénario 2 insérées (1 markdown + 4 code). Notebook exécuté (nbconvert exit 0, 36 cellules). 3 PNG (sc2_dag, sc2_coeff, sc2_bar) et 1 CSV (sc2_mediateur.csv) produits. OLS sans médiateur > OLS avec médiateur (biais vers le bas visible). Seeds SEED+40 et SEED+41 utilisés.</done>
</task>

</tasks>

<verification>
Vérifications globales du plan 04-01 :

1. `jupyter nbconvert --to notebook --execute formation_causalite.ipynb --output /tmp/test_04_01.ipynb --ExecutePreprocessor.timeout=300` — exit code 0
2. `ls figures/sc2_dag.png figures/sc2_coeff.png figures/sc2_bar.png` — 3 fichiers présents
3. `ls data/sc2_mediateur.csv` — CSV présent
4. Print dans le notebook : OLS sans médiateur ≈ 30.8% > OLS avec panier_moyen ≈ 21.1% (biais visible ~9pp)
5. DAG contient 4 noeuds et 4 arêtes (pas d'arête directe Pub→Ventes)
</verification>

<success_criteria>
- 5 cellules scénario 2 insérées (1 markdown + 4 code)
- 3 PNG exportés dans figures/ : sc2_dag.png, sc2_coeff.png, sc2_bar.png
- 1 CSV exporté dans data/ : sc2_mediateur.csv
- nbconvert exit 0 sur le notebook complet (36 cellules)
- OLS sans médiateur (≈30.8%) > OLS avec panier_moyen (≈21.1%) — biais pédagogiquement visible
- DAG diamond layout : 4 noeuds (Pub, Nb visites, Panier moyen, Ventes), 4 arêtes, PAS de Pub→Ventes direct
- Seeds isolés : rng_sc2 = SEED+40, rng_cf_sc2 = SEED+41
- xlabel/ylabel : "Uplift log des ventes (≈ %)" cohérent avec Phase 3
</success_criteria>

<output>
After completion, create `.planning/phases/04-scenario2-mediateur/04-01-SUMMARY.md`
</output>
