---
phase: 03-sc-narios-1a-1b-1c-biais-de-s-lection
plan: "02"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - formation_causalite.ipynb
autonomous: false
requirements: [SC1-03]

must_haves:
  truths:
    - "Le scénario 1c affiche un DAG 3 noeuds (Pub, Ventes, Saison) avec layout fixe, exporté figures/sc1c_dag.png"
    - "Le scénario 1c montre un OLS naïf (sans C(mois)) qui surestime l'ATT — biais saisonnier visible"
    - "L'OLS ajusté du scénario 1c inclut C(mois) et se rapproche de l'ATT"
    - "Coefficient plot et bar chart exportés figures/sc1c_coeff.png et figures/sc1c_bar.png"
    - "CSV exporté data/sc1c_selection_saison.csv"
    - "Le formateur valide visuellement les 9 figures (DAG, coeff, bar pour 1a, 1b, 1c)"
    - "nbconvert exit 0 sur le notebook complet"
  artifacts:
    - path: "figures/sc1c_dag.png"
      provides: "DAG Scénario 1c"
    - path: "figures/sc1c_coeff.png"
      provides: "Coefficient plot Scénario 1c"
    - path: "figures/sc1c_bar.png"
      provides: "Bar chart Scénario 1c"
    - path: "data/sc1c_selection_saison.csv"
      provides: "Dataset scénario 1c"
  key_links:
    - from: "code-sc1c-data"
      to: "df_sc1c['pub']"
      via: "assignation niveau ligne (pas niveau magasin) — varie par mois"
      pattern: "rng_sc1c\\.binomial\\(1, probs_1c\\)"
    - from: "code-sc1c-data"
      to: "model_naive_1c"
      via: "ventes ~ pub — SANS C(mois) pour rendre biais visible"
      pattern: "ventes ~ pub'"
    - from: "code-sc1c-data"
      to: "model_adj_1c"
      via: "ventes ~ pub + C(mois) — contrôle la saison"
      pattern: "C\\(mois\\)"
---

<objective>
Insérer le scénario 1c (sélection par saison) dans le notebook, exécuter le notebook complet, puis faire valider visuellement les 9 figures par le formateur. Le scénario 1c est structurellement différent de 1a/1b : l'assignation pub se fait au niveau ligne (par mois), le naïf n'inclut PAS C(mois), et l'ajusté l'inclut.

Purpose: Compléter la triade des biais de sélection. Démontrer que le biais saisonnier est visible quand on omet les dummies mois dans le naïf, et qu'il disparaît quand on les inclut.

Output: 3 PNG (sc1c_dag.png, sc1c_coeff.png, sc1c_bar.png), 1 CSV, 5 nouvelles cellules dans le notebook (1 markdown + 4 code). Validation humaine des 9 figures totales.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-sc-narios-1a-1b-1c-biais-de-s-lection/03-CONTEXT.md
@.planning/phases/03-sc-narios-1a-1b-1c-biais-de-s-lection/03-RESEARCH.md
@.planning/phases/03-sc-narios-1a-1b-1c-biais-de-s-lection/03-01-SUMMARY.md

<interfaces>
<!-- Différence architecturale CRITIQUE entre 1c et 1a/1b -->

```
Scénarios 1a et 1b (niveau magasin) :
  - pub assigné par magasin (constant sur 24 mois)
  - drop_duplicates('magasin_id') avant assignation
  - treated_ids = liste de magasin_id
  - ATT : filtrer df_sc1x[df_sc1x['magasin_id'].isin(treated_ids)]

Scénario 1c (niveau ligne) :
  - pub assigné par ligne (varie par mois selon saison)
  - PAS de drop_duplicates — opère directement sur le panel 4800 lignes
  - treated_rows = df_sc1c[df_sc1c['pub'] == 1] (lignes, pas magasins)
  - ATT : filtrer df_sc1c[df_sc1c['pub'] == 1] directement

OLS spécifique 1c (DIFFERENT de 1a/1b) :
  - OLS naïf 1c : 'ventes ~ pub'          (SANS C(mois) — biais visible)
  - OLS ajusté 1c : 'ventes ~ pub + C(mois)' (AVEC C(mois) — contrôle saison)
```

<!-- Critère haute saison pour 1c -->

```python
# EFFET_SAISON positif pour mois : 4, 5, 6, 7, 8, 11, 12 (7 mois sur 12)
# Critère : effet_saison_val > 0
haute_mask = df_sc1c['effet_saison_val'] > 0
probs_1c = np.where(haute_mask, P_PUB_HAUTE_SAISON, P_PUB_BASSE_SAISON)
# P_PUB_HAUTE_SAISON = 0.70, P_PUB_BASSE_SAISON = 0.30
```

<!-- Seeds dédiés -->

```python
rng_sc1c = np.random.default_rng(SEED + 30)  # assignation pub + outcomes observés
rng_cf_1c = np.random.default_rng(SEED + 31)  # outcomes contrefactuels (indépendant)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insérer les 5 cellules du scénario 1c et exécuter le notebook</name>
  <files>formation_causalite.ipynb</files>
  <action>
Insérer 5 cellules après les cellules du plan 03-01 (à la fin du notebook actuel).

**Cellule 1 — Markdown section 1c :**
```
## Scénario 1c — Biais de sélection par saison

Le confondant `saison` détermine à la fois la probabilité de recevoir la pub (haute saison = plus de pub) et le niveau des ventes. L'assignation pub varie par mois (niveau ligne). Le modèle naïf omet les dummies de mois, rendant le biais saisonnier visible.
```

**Cellule 2 — code-sc1c-data :**
```python
# ─────────────────────────────────────────────
# Scénario 1c — Sélection par saison
# Confondant : effet_saison_val (via mois)
# Assignation pub : niveau LIGNE (varie par mois) — différent de 1a/1b
# ─────────────────────────────────────────────

rng_sc1c = np.random.default_rng(SEED + 30)

# Assignation pub au niveau ligne (4800 lignes) — haute saison = pub plus probable
df_sc1c = base_df.copy()
haute_mask = df_sc1c['effet_saison_val'] > 0
probs_1c = np.where(haute_mask, P_PUB_HAUTE_SAISON, P_PUB_BASSE_SAISON)
df_sc1c['pub'] = rng_sc1c.binomial(1, probs_1c)
df_sc1c = compute_outcomes(df_sc1c, PARAMS, rng_sc1c)

# Calcul ATT contrefactuel — traités = lignes avec pub=1 (pas des magasins)
df_treated_1c = df_sc1c[df_sc1c['pub'] == 1].copy()
rng_cf_1c = np.random.default_rng(SEED + 31)
df_cf_1c = df_treated_1c.copy()
df_cf_1c['pub'] = 0
df_cf_1c = compute_outcomes(df_cf_1c, PARAMS, rng_cf_1c)
att_1c = (df_treated_1c['ventes'].values - df_cf_1c['ventes'].values).mean()

# OLS naïf : SANS C(mois) — pour rendre le biais saisonnier visible
model_naive_1c = smf.ols('ventes ~ pub', data=df_sc1c).fit()
# OLS ajusté : AVEC C(mois) — contrôle la saison
model_adj_1c   = smf.ols('ventes ~ pub + C(mois)', data=df_sc1c).fit()

# Export CSV
df_sc1c.to_csv('data/sc1c_selection_saison.csv', index=False)
print(f"Sc1c — Lignes pub=1 : {df_sc1c['pub'].sum()}/4800")
print(f"Sc1c — ATT: {att_1c:.1f} €  |  OLS naïf (sans mois): {model_naive_1c.params['pub']:.1f} €  |  OLS ajusté (C(mois)): {model_adj_1c.params['pub']:.1f} €")
```

**Cellule 3 — code-sc1c-dag :**
```python
# ─────────────────────────────────────────────
# Figure DAG — Scénario 1c
# ─────────────────────────────────────────────

G_1c = nx.DiGraph()
G_1c.add_edges_from([
    ('Saison', 'Pub'),
    ('Saison', 'Ventes'),
    ('Pub', 'Ventes'),
])
pos_1c = {'Pub': (0, 0), 'Ventes': (2, 0), 'Saison': (1, 1)}

color_map_1c = {'Pub': 'steelblue', 'Ventes': 'seagreen', 'Saison': 'darkorange'}
node_colors_1c = [color_map_1c[n] for n in G_1c.nodes()]

fig, ax = plt.subplots(figsize=(5, 3))
nx.draw_networkx(G_1c, pos_1c, ax=ax,
                 node_color=node_colors_1c,
                 node_size=2000, font_size=9, font_color='white',
                 arrows=True, arrowsize=20)
ax.axis('off')
ax.set_title('DAG — Scénario 1c : sélection par saison')
fig.savefig('figures/sc1c_dag.png', dpi=150, bbox_inches='tight')
plt.show()
```

**Cellule 4 — code-sc1c-coeff :**
```python
# ─────────────────────────────────────────────
# Figure coefficient plot — Scénario 1c
# ─────────────────────────────────────────────

coef_naive_1c = model_naive_1c.params['pub']
ci_naive_1c   = model_naive_1c.conf_int()
lower_naive_1c = ci_naive_1c.loc['pub', 0]
upper_naive_1c = ci_naive_1c.loc['pub', 1]

coef_adj_1c = model_adj_1c.params['pub']
ci_adj_1c   = model_adj_1c.conf_int()
lower_adj_1c = ci_adj_1c.loc['pub', 0]
upper_adj_1c = ci_adj_1c.loc['pub', 1]

xerr_naive_1c = [[coef_naive_1c - lower_naive_1c], [upper_naive_1c - coef_naive_1c]]
xerr_adj_1c   = [[coef_adj_1c   - lower_adj_1c],   [upper_adj_1c   - coef_adj_1c]]

estimators_1c = ['OLS naïf (sans mois)', 'OLS ajusté + C(mois)', 'Valeur vraie (ATT)']
y_pos_1c = [2, 1, 0]

fig, ax = plt.subplots(figsize=(8, 4))
ax.errorbar(coef_naive_1c, 2, xerr=xerr_naive_1c, fmt='o', capsize=5,
            color='#e74c3c', label='OLS naïf (sans mois)', markersize=8)
ax.errorbar(coef_adj_1c, 1, xerr=xerr_adj_1c, fmt='o', capsize=5,
            color='#3498db', label='OLS ajusté + C(mois)', markersize=8)
ax.errorbar(att_1c, 0, fmt='D', color='#2ecc71', label='Valeur vraie (ATT)', markersize=10)
ax.axvline(x=att_1c, color='gray', linestyle='--', alpha=0.5)
ax.set_yticks(y_pos_1c)
ax.set_yticklabels(estimators_1c)
ax.set_xlabel('Effet estimé de la pub sur les ventes (€)')
ax.set_title('Scénario 1c — Coefficients OLS naïf vs ajusté vs valeur vraie')
ax.legend(loc='lower right')
fig.savefig('figures/sc1c_coeff.png', dpi=150, bbox_inches='tight')
plt.show()
```

**Cellule 5 — code-sc1c-bar :**
```python
# ─────────────────────────────────────────────
# Figure bar chart — Scénario 1c
# ─────────────────────────────────────────────

fig, ax = plt.subplots(figsize=(7, 4))
labels_1c = ['OLS naïf (sans mois)', 'OLS ajusté + C(mois)', 'Valeur vraie (ATT)']
values_1c = [coef_naive_1c, coef_adj_1c, att_1c]
colors_est_1c = ['#e74c3c', '#3498db', '#2ecc71']

ax.bar(range(len(labels_1c)), values_1c, color=colors_est_1c, width=0.5)
ax.set_xticks(range(len(labels_1c)))
ax.set_xticklabels(labels_1c)
ax.set_ylabel('Effet estimé de la pub sur les ventes (€)')
ax.set_title('Scénario 1c — Comparaison des estimateurs')
ax.axhline(y=att_1c, color='gray', linestyle='--', alpha=0.5)
fig.savefig('figures/sc1c_bar.png', dpi=150, bbox_inches='tight')
plt.show()
```

Après insertion des cellules, exécuter le notebook complet :
```bash
jupyter nbconvert --to notebook --execute formation_causalite.ipynb \
    --output formation_causalite.ipynb \
    --ExecutePreprocessor.timeout=300
```

Anti-patterns CRITIQUES pour 1c :
- PAS de `drop_duplicates` — assignation directement sur les 4800 lignes du panel
- OLS naïf = `'ventes ~ pub'` (PAS `+ C(mois)`) — c'est l'exception structurelle de 1c
- `rng_cf_1c = np.random.default_rng(SEED + 31)` — seed dédié pour le contrefactuel
- `ci.loc['pub', 0]` et `ci.loc['pub', 1]` (entiers, pas strings)
- `fig.savefig(...)` AVANT `plt.show()` dans chaque cellule
  </action>
  <verify>
    <automated>python -c "
import json, os, subprocess

# 1. Vérifier cellules 1c présentes
with open('formation_causalite.ipynb') as f:
    nb = json.load(f)
sources = [''.join(c['source']) for c in nb['cells']]
all_src = '\n'.join(sources)

checks = {
    'sc1c_data present': 'rng_sc1c' in all_src,
    'sc1c_dag present': 'G_1c' in all_src,
    'sc1c_coeff present': 'coef_naive_1c' in all_src,
    'sc1c_bar present': 'values_1c' in all_src,
    'naif sans mois': \"'ventes ~ pub'\" in all_src,
    'ajuste avec mois': 'ventes ~ pub + C(mois)' in all_src,
    'ligne level (not magasin)': 'haute_mask' in all_src,
    'nb >= 31 cells': len(nb['cells']) >= 31,
    'sc1c_dag.png exported': 'sc1c_dag.png' in all_src,
    'sc1c_coeff.png exported': 'sc1c_coeff.png' in all_src,
    'sc1c_bar.png exported': 'sc1c_bar.png' in all_src,
}
for k, v in checks.items():
    print(f'{\"OK\" if v else \"FAIL\"}: {k}')
print(f'Total cells: {len(nb[\"cells\"])}')

# 2. Vérifier les PNG exportés
pngs = ['figures/sc1c_dag.png', 'figures/sc1c_coeff.png', 'figures/sc1c_bar.png']
for p in pngs:
    exists = os.path.exists(p)
    print(f'{\"OK\" if exists else \"FAIL\"}: {p} exists')
    checks[p] = exists

# 3. Vérifier le CSV
csv_ok = os.path.exists('data/sc1c_selection_saison.csv')
print(f'{\"OK\" if csv_ok else \"FAIL\"}: data/sc1c_selection_saison.csv exists')
checks['csv'] = csv_ok

assert all(checks.values()), 'One or more checks failed'
print('PASS')
"
</automated>
  </verify>
  <done>5 cellules insérées pour scénario 1c. Notebook exécuté (nbconvert exit 0). 3 PNG et 1 CSV produits. OLS naïf 1c utilise ventes ~ pub (sans mois). Seeds dédiés SEED+30/+31.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Scénarios 1a, 1b et 1c complets dans le notebook :
- 9 figures exportées dans figures/ : sc1a_dag, sc1a_coeff, sc1a_bar, sc1b_dag, sc1b_coeff, sc1b_bar, sc1c_dag, sc1c_coeff, sc1c_bar
- 3 CSV exportés dans data/ : sc1a_selection_qualite.csv, sc1b_selection_urbain.csv, sc1c_selection_saison.csv
- Pour chaque scénario : OLS naïf > ATT (surestimation pédagogiquement visible)
- Scénario 1c : OLS naïf sans C(mois) montre le biais saisonnier
  </what-built>
  <how-to-verify>
1. Ouvrir le notebook `formation_causalite.ipynb` dans Jupyter
2. Vérifier visuellement les 9 figures :

   **DAGs (sc1a_dag, sc1b_dag, sc1c_dag) :**
   - 3 nœuds avec flèches (Pub → Ventes, Confondant → Pub, Confondant → Ventes)
   - Nœuds colorés (bleu Pub, vert Ventes, orange Confondant)
   - Titre factuel (pas de commentaire interprétatif)
   - Layout lisible (pas de nœuds qui se chevauchent)

   **Coefficient plots (sc1a_coeff, sc1b_coeff, sc1c_coeff) :**
   - 3 points sur l'axe Y (OLS naïf, OLS ajusté, Valeur vraie/ATT)
   - Barres d'erreur IC 95% sur les deux estimateurs OLS
   - OLS naïf (rouge) est à droite de la valeur vraie (ATT, vert)
   - Ligne pointillée verticale à la valeur vraie

   **Bar charts (sc1a_bar, sc1b_bar, sc1c_bar) :**
   - 3 barres colorées (rouge, bleu, vert)
   - Barre OLS naïf (rouge) est la plus haute
   - Ligne pointillée horizontale à la valeur vraie

3. Vérifier les outputs des cellules code-sc1x-data (print) :
   - Sc1a : ATT environ 1000-1200 €, OLS naïf environ 1500-1700 € (surestimation ~50%)
   - Sc1b : ATT environ 1000-1300 €, OLS naïf supérieur à l'ATT
   - Sc1c : ATT environ 1100-1300 €, OLS naïf (sans mois) supérieur à l'ATT
  </how-to-verify>
  <resume-signal>Tapez "approved" si les 9 figures sont correctes, ou décrivez les problèmes observés</resume-signal>
</task>

</tasks>

<verification>
Vérifications globales du plan 03-02 :

1. `jupyter nbconvert --to notebook --execute formation_causalite.ipynb --output /tmp/test_03_02.ipynb --ExecutePreprocessor.timeout=300` — exit code 0
2. `ls figures/sc1c_dag.png figures/sc1c_coeff.png figures/sc1c_bar.png` — 3 fichiers présents
3. `ls data/sc1c_selection_saison.csv` — CSV présent
4. Print dans le notebook : ATT < OLS naïf pour 1c (biais visible)
5. Checkpoint humain approuvé par le formateur — 9 figures validées visuellement
</verification>

<success_criteria>
- 5 cellules scénario 1c insérées (1 markdown + 4 code)
- 3 PNG sc1c exportés dans figures/
- 1 CSV sc1c exporté dans data/
- nbconvert exit 0 sur le notebook complet (31+ cellules)
- OLS naïf 1c (sans C(mois)) > ATT (biais saisonnier visible)
- OLS ajusté 1c (avec C(mois)) convergé vers ATT
- Formateur a approuvé visuellement les 9 figures des 3 sous-scénarios
</success_criteria>

<output>
After completion, create `.planning/phases/03-sc-narios-1a-1b-1c-biais-de-s-lection/03-02-SUMMARY.md`
</output>
