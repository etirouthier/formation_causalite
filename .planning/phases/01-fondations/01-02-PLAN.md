---
phase: 01-fondations
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - formation_causalite.ipynb
autonomous: true
requirements:
  - INFRA-03
  - DGP-01
  - DGP-02
  - DGP-03
  - DGP-04
  - DGP-05
  - DGP-06

must_haves:
  truths:
    - "Deux exécutions consécutives de Restart & Run All produisent exactement le même DataFrame base_df"
    - "base_df contient N_MAGASINS × T_MOIS lignes avec colonnes magasin_id, mois, taille, n_potentiel, urbain, qualite_equipe, effet_saison_val"
    - "compute_outcomes() calcule p_visite clippée [0.01, 0.99], nb_visites Binomial, ventes et panier_moyen"
    - "La cellule Assertions passe sans exception avec les paramètres par défaut"
    - "Le pattern DAG avec pos dict fixe est démontré dans une cellule (sans spring_layout)"
    - "pub est absent de base_df — chaque scénario assigne pub sur une copie base_df.copy()"
  artifacts:
    - path: "formation_causalite.ipynb"
      provides: "generate_base_panel, compute_outcomes, assertions, DAG pattern cell"
      contains: "generate_base_panel, compute_outcomes, np.clip, assert, nx.DiGraph"
    - path: "data/base_panel.csv"
      provides: "Export CSV du panel de base pour vérification"
      min_lines: 4800
  key_links:
    - from: "generate_base_panel(PARAMS, rng)"
      to: "base_df"
      via: "cross join magasins × mois, vectorisé"
      pattern: "merge.*cross"
    - from: "compute_outcomes(df, PARAMS, rng)"
      to: "p_visite, nb_visites, ventes, panier_moyen"
      via: "np.clip puis rng.binomial"
      pattern: "np\\.clip.*0\\.01.*0\\.99"
    - from: "Cellule Assertions"
      to: "base_df['p_visite']"
      via: "assert .between(0.01, 0.99).all()"
      pattern: "assert.*between"
    - from: "nx.DiGraph"
      to: "pos dict fixe"
      via: "coordonnées manuelles — pas spring_layout"
      pattern: "pos\\s*=\\s*\\{"
---

<objective>
Ajouter au notebook les fonctions DGP (generate_base_panel, compute_outcomes), la cellule d'exécution du panel de base, la cellule d'assertions, et une cellule de démonstration du pattern DAG.

Purpose: Ces fonctions sont le coeur de la formation — elles génèrent les données que tous les scénarios (Phases 2-5) réutilisent. La cellule d'assertions garantit que les paramètres par défaut produisent des données pédagogiquement valides sans intervention du formateur.

Output: Notebook complet pour Phase 1 — exécutable de bout en bout via Restart & Run All, avec export CSV du panel de base et assertions validées.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-fondations/01-RESEARCH.md
@.planning/phases/01-fondations/01-01-SUMMARY.md

**Contrainte absolue :** Livrable = cellules .ipynb. Toutes les tâches modifient formation_causalite.ipynb en ajoutant des cellules au JSON.

**Décisions locked :**
- `pub` N'APPARTIENT PAS à generate_base_panel — chaque scénario assigne pub sur base_df.copy()
- np.clip(p_visite, 0.01, 0.99) OBLIGATOIRE avant tout rng.binomial()
- DAGs via networkx avec pos dict fixe — jamais spring_layout
- CLT approximation pour ventes : ventes ~ Normal(n*mu, sqrt(n)*sigma) — vectorisé, acceptable

<interfaces>
<!-- Contrats extraits de RESEARCH.md — l'exécuteur implémente exactement ces signatures -->

**generate_base_panel(params, rng) — retourne DataFrame caractéristiques uniquement :**
```python
def generate_base_panel(params, rng):
    """
    Génère le panel partagé N_magasins × T_mois.
    Retourne : DataFrame avec colonnes :
        magasin_id, taille, n_potentiel, urbain, qualite_equipe, mois, effet_saison_val
    NB : pas de colonnes pub, nb_visites, ventes, panier_moyen — assignées par chaque scénario.
    """
    n = params['n_magasins']
    t = params['t_mois']
    n_potentiel_map = {
        'petit': params['n_petit'],
        'moyen': params['n_moyen'],
        'grand': params['n_grand'],
    }
    # Caractéristiques fixes (vectorisé)
    magasins = pd.DataFrame({
        'magasin_id': range(n),
        'taille': rng.choice(['petit', 'moyen', 'grand'], size=n, p=[0.4, 0.4, 0.2]),
        'urbain': rng.integers(0, 2, size=n),   # binaire 0/1
        'qualite_equipe': rng.integers(0, 2, size=n),
    })
    magasins['n_potentiel'] = magasins['taille'].map(n_potentiel_map)
    # Panel magasin × mois (cross join pandas 2.x)
    mois_df = pd.DataFrame({'mois': range(1, t + 1)})
    base = magasins.merge(mois_df, how='cross')
    base['effet_saison_val'] = base['mois'].map(params['effet_saison'])
    return base.reset_index(drop=True)
```

**compute_outcomes(df, params, rng) — modifie df en place, retourne df :**
```python
def compute_outcomes(df, params, rng):
    """
    Calcule p_visite, nb_visites, ventes, panier_moyen.
    Entrée : df avec colonnes taille, urbain, qualite_equipe, effet_saison_val, pub.
    p_visite est clippée à [0.01, 0.99] — OBLIGATOIRE avant rng.binomial().
    Approximation CLT pour ventes : ventes ~ Normal(nb_visites * mu, sqrt(nb_visites) * sigma).
    """
    p_visite = (
        params['p_base_visite']
        + params['effet_urbain'] * df['urbain']
        + params['effet_equipe'] * df['qualite_equipe']
        + df['effet_saison_val']
        + params['effet_pub_visites'] * df['pub']
    )
    p_visite = np.clip(p_visite, 0.01, 0.99)   # OBLIGATOIRE
    df = df.copy()
    df['p_visite'] = p_visite
    df['nb_visites'] = rng.binomial(df['n_potentiel'].values, p_visite.values)
    mu_panier = params['mu_panier_base'] * (1 + params['effet_pub_panier'] * df['pub'])
    df['ventes'] = (
        df['nb_visites'] * mu_panier
        + rng.normal(0, params['sigma_panier'], len(df)) * np.sqrt(df['nb_visites'])
    )
    df['panier_moyen'] = np.where(
        df['nb_visites'] > 0,
        df['ventes'] / df['nb_visites'],
        np.nan
    )
    return df
```

**Assertions attendues (INFRA-03) :**
```python
# 1. Cohérence paramétrique — p_visite ne peut dépasser 0.99
max_p = (PARAMS['p_base_visite'] + PARAMS['effet_urbain'] + PARAMS['effet_equipe']
         + max(PARAMS['effet_saison'].values()) + PARAMS['effet_pub_visites'])
assert max_p < 0.99, f"p_visite max cumulée ({max_p:.3f}) >= 0.99"

# 2. p_visite dans le DataFrame
assert base_df['p_visite'].between(0.01, 0.99).all(), "p_visite hors [0.01, 0.99]"

# 3. Pas de nb_visites = 0 (signalerait p_base trop faible)
assert base_df['nb_visites'].min() > 0, "nb_visites=0 détecté"

# 4. Variance ratio petit/grand > 2x (pédagogiquement visible)
std_by_taille = base_df.groupby('taille')['panier_moyen'].std()
ratio = std_by_taille['petit'] / std_by_taille['grand']
assert ratio > 2.0, f"Variance ratio petit/grand = {ratio:.1f}x < 2.0 cible"

# 5. pub absent de base_df
assert 'pub' not in base_df.columns, "pub ne doit pas etre dans base_df"

print(f"Assertions OK — max p_visite cumulee: {max_p:.3f}, variance ratio: {ratio:.1f}x")
```

**Pattern DAG (DGP-06) — cellule de démonstration :**
```python
# Démonstration du pattern DAG utilisé dans tous les scénarios
G = nx.DiGraph()
G.add_edges_from([
    ('pub', 'ventes'),
    ('confondant', 'pub'),
    ('confondant', 'ventes'),
])
# FIXE — jamais spring_layout
pos = {'pub': (0, 0), 'ventes': (2, 0), 'confondant': (1, 1)}

fig, ax = plt.subplots(figsize=(5, 3))
nx.draw_networkx(G, pos, ax=ax,
                 node_color=['steelblue', 'seagreen', 'darkorange'],
                 node_size=2000, font_size=10, font_color='white',
                 arrows=True, arrowsize=20)
ax.axis('off')
ax.set_title('Pattern DAG — exemple (scénario 1a : sélection par équipe)')
fig.savefig('figures/dag_pattern_demo.png', dpi=150, bbox_inches='tight')
plt.show()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ajouter les fonctions generate_base_panel et compute_outcomes</name>
  <files>formation_causalite.ipynb</files>
  <action>
Charger formation_causalite.ipynb (JSON) et ajouter deux cellules code après la cellule Imports existante (index 3 et 4) :

**Cellule 3 — Markdown (séparateur de section) :**
```markdown
---
## Générateur de panel partagé

Fonctions utilisées par tous les scénarios. `generate_base_panel` produit les caractéristiques structurelles. `compute_outcomes` calcule les variables d'outcome pour un DataFrame avec `pub` assigné.
```

**Cellule 4 — Code (fonctions DGP) :**
Contient la définition complète de `generate_base_panel(params, rng)` et `compute_outcomes(df, params, rng)` telles que spécifiées dans la section `<interfaces>` ci-dessus.

Points critiques à respecter exactement :
- `rng.integers(0, 2, size=n)` pour les variables binaires (pas `rng.binomial(1, 0.5, n)` qui fonctionne aussi mais est moins idiomatique pour des Bernoulli 0.5)
- `how='cross'` dans le merge (pandas 2.x)
- `np.clip(p_visite, 0.01, 0.99)` AVANT `rng.binomial()`
- `df = df.copy()` en début de compute_outcomes pour éviter SettingWithCopyWarning
- `panier_moyen = np.where(nb_visites > 0, ventes / nb_visites, np.nan)` pour gérer le cas nb_visites=0
- Aucun magic number dans ces fonctions — tous les paramètres viennent de `params`

**Cellule 5 — Code (exécution + export) :**
```python
# ─────────────────────────────────────────────
# Génération du panel de base (pub=0 partout)
# ─────────────────────────────────────────────
base_df = generate_base_panel(PARAMS, rng)

# Ajouter pub=0 pour calculer les outcomes de référence
base_df['pub'] = 0
base_df = compute_outcomes(base_df, PARAMS, rng)
# Retirer pub du panel de base après calcul des outcomes (chaque scénario réassigne)
base_df = base_df.drop(columns=['pub'])

print(f"Panel de base : {len(base_df)} lignes × {len(base_df.columns)} colonnes")
print(f"Colonnes : {list(base_df.columns)}")
print(base_df.head(3))

# Export CSV
base_df.to_csv('data/base_panel.csv', index=False)
print("\nExporte : data/base_panel.csv")
```

Attention : la cellule d'exécution reconstruit base_df SANS colonne pub à la fin (drop), pour que chaque scénario parte d'un DataFrame propre (caractéristiques + outcomes baseline seulement).
  </action>
  <verify>
    <automated>python -c "
import json
nb = json.load(open('formation_causalite.ipynb'))
cells = nb['cells']
# Chercher les fonctions dans les cellules code
all_code = ' '.join(''.join(c['source']) for c in cells if c['cell_type']=='code')
assert 'generate_base_panel' in all_code, 'generate_base_panel manquant'
assert 'compute_outcomes' in all_code, 'compute_outcomes manquant'
assert 'np.clip' in all_code, 'np.clip manquant'
assert 'how=.cross.' in all_code or \"how='cross'\" in all_code, 'cross join manquant'
assert 'np.random.seed' not in all_code, 'np.random.seed() interdit'
print(f'OK — {len(cells)} cellules, fonctions DGP presentes')
"</automated>
  </verify>
  <done>generate_base_panel et compute_outcomes sont définies dans le notebook. np.clip présent. Cross join vectorisé utilisé. Aucun magic number dans les fonctions.</done>
</task>

<task type="auto">
  <name>Task 2: Ajouter cellule Assertions et cellule DAG pattern</name>
  <files>formation_causalite.ipynb</files>
  <action>
Ajouter deux cellules supplémentaires au notebook (après les cellules DGP) :

**Cellule N — Markdown (séparateur) :**
```markdown
---
## Validation — Assertions automatiques

Vérifie que les paramètres par défaut produisent des données cohérentes. Cette cellule doit s'exécuter sans exception.
```

**Cellule N+1 — Code (assertions INFRA-03) :**
Contient exactement le bloc d'assertions spécifié dans `<interfaces>` ci-dessus :
1. `max_p < 0.99` — cohérence paramétrique (empêche p_visite > 1)
2. `base_df['p_visite'].between(0.01, 0.99).all()` — valeurs dans les bornes
3. `base_df['nb_visites'].min() > 0` — pas de lignes sans visites
4. `ratio > 2.0` — variance ratio petit/grand visible (pédagogique)
5. `'pub' not in base_df.columns` — base_df reste propre

Afficher un message de succès avec les valeurs diagnostiques après les assertions.

**Cellule N+2 — Markdown (séparateur) :**
```markdown
---
## Pattern DAG

Démonstration du pattern NetworkX utilisé dans tous les scénarios. Layout fixe via `pos` dict — jamais `spring_layout`.
```

**Cellule N+3 — Code (pattern DAG DGP-06) :**
Contient exactement le code DAG spécifié dans `<interfaces>` ci-dessus :
- `G = nx.DiGraph()` avec edges pub→ventes, confondant→pub, confondant→ventes
- `pos` dict avec coordonnées fixes manuelles (pas spring_layout)
- `nx.draw_networkx` avec couleurs différentes par noeud
- `fig.savefig('figures/dag_pattern_demo.png', dpi=150, bbox_inches='tight')` AVANT `plt.show()`
- Export PNG dans figures/ avec dpi=150

CRITIQUE : `fig.savefig(...)` doit précéder `plt.show()` — dans l'ordre inverse, le PNG sera blanc.
  </action>
  <verify>
    <automated>python -c "
import json
nb = json.load(open('formation_causalite.ipynb'))
all_code = ' '.join(''.join(c['source']) for c in nb['cells'] if c['cell_type']=='code')
# Assertions présentes
assert 'assert' in all_code, 'assertions manquantes'
assert 'between(0.01, 0.99)' in all_code, 'assertion p_visite manquante'
# DAG présent
assert 'nx.DiGraph' in all_code, 'nx.DiGraph manquant'
assert 'spring_layout' not in all_code, 'spring_layout interdit'
assert 'savefig' in all_code, 'savefig manquant'
# Vérifier ordre savefig avant show dans le source
for c in nb['cells']:
    if c['cell_type'] == 'code':
        src = ''.join(c['source'])
        if 'savefig' in src and 'plt.show' in src:
            idx_save = src.index('savefig')
            idx_show = src.index('plt.show')
            assert idx_save < idx_show, f'savefig APRES plt.show dans une cellule — PNG sera blanc'
print(f'OK — {len(nb[\"cells\"])} cellules au total, assertions et DAG valides')
"</automated>
  </verify>
  <done>
La cellule Assertions s'exécute sans exception avec PARAMS par défaut. Les 5 assertions (max_p, p_visite bounds, nb_visites > 0, variance ratio, pub absent) passent toutes. La cellule DAG produit figures/dag_pattern_demo.png avec layout fixe. savefig précède plt.show dans toutes les cellules.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Notebook formation_causalite.ipynb complet pour Phase 1 :
- Cellule Paramètres (ALL_CAPS + PARAMS dict)
- Cellule Imports (rng, mkdir figures/ data/)
- Fonctions generate_base_panel et compute_outcomes
- Cellule d'exécution (base_df, export CSV)
- Cellule Assertions (5 checks automatiques)
- Cellule DAG pattern (figures/dag_pattern_demo.png)
  </what-built>
  <how-to-verify>
Dans JupyterLab (ou équivalent) :

1. Ouvrir formation_causalite.ipynb
2. Kernel → Restart & Run All
3. Vérifier que toutes les cellules s'exécutent sans erreur (aucune exception visible)
4. Vérifier que la cellule Assertions affiche "Assertions OK" avec les valeurs diagnostiques
5. Vérifier que data/base_panel.csv existe et contient ~4800 lignes (200 magasins × 24 mois)
6. Vérifier que figures/dag_pattern_demo.png existe et n'est pas un PNG blanc

En ligne de commande (alternative sans JupyterLab) :
```bash
jupyter nbconvert --to notebook --execute formation_causalite.ipynb --output /tmp/test_phase1.ipynb
echo "Exit code: $?"
wc -l data/base_panel.csv
file figures/dag_pattern_demo.png
```
  </how-to-verify>
  <resume-signal>Taper "approved" si Restart & Run All passe sans erreur et les assertions s'affichent OK. Sinon décrire l'erreur observée.</resume-signal>
</task>

</tasks>

<verification>
Critères objectifs de complétion de la Phase 1 :

```bash
# 1. Notebook existe et est valid JSON
python -c "import json; nb=json.load(open('formation_causalite.ipynb')); print(f'{len(nb[\"cells\"])} cellules')"

# 2. Aucun magic number hors cellule Paramètres
python -c "
import json
nb = json.load(open('formation_causalite.ipynb'))
params_cell = ''.join(nb['cells'][1]['source'])
other_cells = [c for c in nb['cells'][2:] if c['cell_type']=='code']
import re
for i, c in enumerate(other_cells):
    src = ''.join(c['source'])
    # Chercher des float/int standalone qui ne sont pas des coordonnées pos ou dpi
    suspects = re.findall(r'(?<![A-Z_])\b(0\.[1-9][0-9]|[1-9][0-9]{1,3})\b(?!\s*[:,])', src)
    if suspects:
        print(f'AVERTISSEMENT cellule {i+2}: suspects {suspects[:5]}')
print('Vérification magic numbers terminée')
"

# 3. Exécution complète
jupyter nbconvert --to notebook --execute formation_causalite.ipynb --output /tmp/phase1_verify.ipynb 2>&1 | grep -E 'ERROR|error|Exception|assert' | head -10

# 4. Exports présents
ls -la data/base_panel.csv figures/dag_pattern_demo.png
```
</verification>

<success_criteria>
1. `Restart & Run All` s'exécute sans erreur ni exception
2. Deux exécutions consécutives produisent exactement le même data/base_panel.csv (seed reproductible)
3. La cellule Assertions affiche "Assertions OK" avec max_p < 0.99 et ratio > 2.0x
4. base_df contient N_MAGASINS × T_MOIS lignes (4800 avec les paramètres par défaut) sans colonne pub
5. figures/dag_pattern_demo.png existe, n'est pas blanc, et affiche un DAG avec layout fixe
6. data/base_panel.csv est exporté avec toutes les colonnes attendues
</success_criteria>

<output>
Après complétion, créer `.planning/phases/01-fondations/01-02-SUMMARY.md` avec :
- Nombre de cellules dans le notebook final
- Valeurs diagnostiques des assertions (max_p, variance ratio obtenu)
- Colonnes de base_df
- Toute déviation par rapport au plan et justification
- Confirmation que Restart & Run All passe
</output>
