---
phase: 08-ventes-par-m2-et-dag-uniforme
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - formation_causalite.ipynb
autonomous: true
requirements:
  - DAG-01

must_haves:
  truths:
    - "Tous les DAG (pattern, sc1a, sc1b, sc1c, sc2, sc3) utilisent uniquement noir pour noeuds, aretes et labels"
    - "Aucune valeur de couleur (steelblue, seagreen, darkorange, crimson, #...) dans les cellules DAG"
    - "Restart & Run All produit toutes les figures PNG DAG sans erreur"
  artifacts:
    - path: "figures/dag_pattern_demo.png"
      provides: "Pattern DAG en noir"
    - path: "figures/sc1a_dag.png"
      provides: "DAG sc1a en noir"
    - path: "figures/sc1b_dag.png"
      provides: "DAG sc1b en noir"
    - path: "figures/sc1c_dag.png"
      provides: "DAG sc1c en noir"
    - path: "figures/sc2_dag.png"
      provides: "DAG sc2 en noir"
    - path: "figures/sc3_dag.png"
      provides: "DAG sc3 en noir"
  key_links:
    - from: "chaque cellule DAG"
      to: "nx.draw_networkx()"
      via: "node_color='black', font_color='white' ou 'black'"
      pattern: "node_color"
---

<objective>
Uniformiser tous les DAG du notebook pour utiliser exclusivement le noir — supprimer toutes les couleurs des noeuds, aretes et labels.

Purpose: Lisibilité universelle (impression N&B, projecteur), cohérence visuelle, et respect de la convention pédagogique "le DAG est un schéma neutre".
Output: 6 fichiers PNG DAG régénérés en noir.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Uniformiser les 6 cellules DAG en noir</name>
  <files>formation_causalite.ipynb</files>
  <action>
Localiser et modifier les 6 cellules DAG suivantes dans le notebook. Pour chacune, appliquer le même pattern noir :

**Cellule `code-dag-pattern` (pattern démonstration) :**
Remplacer `node_color=['steelblue', 'seagreen', 'darkorange']` par `node_color='black'`.
Remplacer `font_color='white'` par `font_color='white'` (garder white pour contraste sur fond noir).
Supprimer tout `color_map` ou liste de couleurs variables.

**Cellule `code-sc1a-dag` (DAG scénario 1a — Qualité équipe) :**
Supprimer `color_map_1a` et `node_colors_1a`.
Dans `nx.draw_networkx()`, remplacer `node_color=node_colors_1a` par `node_color='black'`.
Garder `font_color='white'` pour lisibilité.

**Cellule `code-sc1b-dag` (DAG scénario 1b — Localisation) :**
Supprimer `color_map_1b` et `node_colors_1b`.
Dans `nx.draw_networkx()`, remplacer `node_color=node_colors_1b` par `node_color='black'`.
Garder `font_color='white'`.

**Cellule `code-sc1c-dag` (DAG scénario 1c — Saison) :**
Supprimer `color_map_1c` et `node_colors_1c`.
Dans `nx.draw_networkx()`, remplacer `node_color=node_colors_1c` par `node_color='black'`.
Garder `font_color='white'`.

**Cellule DAG scénario 2 (médiateur) :**
Supprimer `color_map_sc2` et `node_colors_sc2`.
Dans `nx.draw_networkx()`, remplacer `node_color=node_colors_sc2` par `node_color='black'`.
Garder `font_color='white'`.

**Cellule DAG scénario 3 (collider) :**
Supprimer `color_map_sc3` et `node_colors_sc3` (y compris la couleur `'crimson'` du collider).
Dans `nx.draw_networkx()`, remplacer `node_color=node_colors_sc3` par `node_color='black'`.
Garder `font_color='white'`.

Pattern cible pour chaque `nx.draw_networkx()` :
```python
nx.draw_networkx(G, pos, ax=ax,
                 node_color='black',
                 node_size=2000, font_size=9, font_color='white',
                 arrows=True)
```

Ne pas modifier les titres des figures, les positions des noeuds (`pos`), ni la structure des aretes.
  </action>
  <verify>
    <automated>cd /workspaces/formation_causalite && python -c "
import json
with open('formation_causalite.ipynb') as f:
    nb = json.load(f)
dag_cell_ids = ['code-dag-pattern', 'code-sc1a-dag', 'code-sc1b-dag', 'code-sc1c-dag']
colors_to_ban = ['steelblue', 'seagreen', 'darkorange', 'crimson', 'color_map']
found_issues = []
for cell in nb['cells']:
    cid = cell.get('id', '')
    src = ''.join(cell.get('source', []))
    # Check DAG cells by id or by DAG draw pattern
    if 'nx.draw_networkx' in src:
        for banned in colors_to_ban:
            if banned in src:
                found_issues.append(f'Cell {cid}: found banned color {banned}')
if found_issues:
    print('ISSUES:', found_issues)
else:
    print('OK: no banned colors in DAG cells')
"</automated>
  </verify>
  <done>Les 6 cellules DAG ont node_color='black' et aucune couleur variable (steelblue, seagreen, darkorange, crimson, color_map) ne subsiste dans les appels nx.draw_networkx().</done>
</task>

<task type="auto">
  <name>Task 2: Exécuter le notebook et vérifier les figures DAG</name>
  <files>formation_causalite.ipynb</files>
  <action>
Exécuter le notebook complet avec `jupyter nbconvert --to notebook --execute` pour régénérer toutes les figures DAG en noir. Vérifier que les 6 fichiers PNG DAG existent après exécution.

Si la cellule de validation finale liste les figures DAG attendues (dans la liste `figures_attendues`), s'assurer que tous les chemins DAG sont inclus : `figures/dag_pattern_demo.png`, `figures/sc1a_dag.png`, `figures/sc1b_dag.png`, `figures/sc1c_dag.png`, `figures/sc2_dag.png`, `figures/sc3_dag.png`.
  </action>
  <verify>
    <automated>cd /workspaces/formation_causalite && jupyter nbconvert --to notebook --execute formation_causalite.ipynb --output /tmp/test_dag.ipynb 2>&1 | tail -3 && python -c "
import os
dags = ['figures/dag_pattern_demo.png','figures/sc1a_dag.png','figures/sc1b_dag.png','figures/sc1c_dag.png','figures/sc2_dag.png','figures/sc3_dag.png']
missing = [d for d in dags if not os.path.exists(d)]
if missing: print('MISSING:', missing)
else: print('OK: all 6 DAG figures present')
"</automated>
  </verify>
  <done>Notebook s'exécute sans erreur et les 6 figures DAG PNG sont présentes dans figures/.</done>
</task>

</tasks>

<verification>
Après exécution complète :
1. Aucune couleur variable (steelblue, darkorange, crimson, color_map) dans les cellules nx.draw_networkx()
2. Les 6 figures DAG PNG existent dans figures/
3. Notebook s'exécute de bout en bout sans erreur
</verification>

<success_criteria>
- DAG-01 : tous les DAG (6 cellules) utilisent node_color='black' uniquement
- Restart & Run All sans erreur
- Figures DAG regenerées et lisibles en N&B
</success_criteria>

<output>
Après completion, créer `.planning/phases/08-ventes-par-m2-et-dag-uniforme/08-02-SUMMARY.md`
</output>
