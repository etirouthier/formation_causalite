---
phase: 08-ventes-par-m2-et-dag-uniforme
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - formation_causalite.ipynb
autonomous: true
requirements:
  - VM2-01
  - VM2-02

must_haves:
  truths:
    - "La colonne ventes_par_m2 existe dans le dataframe agrégé de base et dans le CSV exporté"
    - "Au moins une figure PNG dans figures/ représente ventes_par_m2 par taille ou localisation"
    - "Restart & Run All termine sans erreur"
  artifacts:
    - path: "formation_causalite.ipynb"
      provides: "Cellule de calcul ventes_par_m2 et cellule figure"
      contains: "ventes_par_m2"
    - path: "data/base_panel.csv"
      provides: "Dataset exporté avec la nouvelle colonne"
      contains: "ventes_par_m2"
    - path: "figures/sc0_ventes_par_m2.png"
      provides: "Figure illustrative de la variable dérivée"
  key_links:
    - from: "cellule calcul ventes_par_m2"
      to: "cellule export CSV"
      via: "df['ventes_par_m2'] présent avant to_csv()"
      pattern: "ventes_par_m2"
---

<objective>
Ajouter la variable dérivée `ventes_par_m2 = ventes / N_potentiel` au notebook et produire au moins une figure illustrative.

Purpose: Permettre au formateur d'interpréter les ventes de façon intensive (euros par visiteur potentiel / m²), indépendamment de la taille du magasin.
Output: Colonne dans le dataset, figure PNG dans figures/.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Calculer ventes_par_m2 et l'inclure dans le CSV exporté</name>
  <files>formation_causalite.ipynb</files>
  <action>
Dans le notebook, localiser la cellule qui agrège les données par magasin (celle qui produit `magasins` avec `n_potentiel` et `ventes`). Après cette agrégation, ajouter :

```python
magasins['ventes_par_m2'] = magasins['ventes'] / magasins['n_potentiel']
```

La variable `n_potentiel` est déjà présente (proxy de la surface du magasin : petit=30, moyen=100, grand=300). Pas de nouveau paramètre DGP nécessaire.

S'assurer que la colonne `ventes_par_m2` est présente dans le dataframe au moment de l'appel `to_csv()` pour `data/base_panel.csv`. Si l'export se fait sur le dataframe `df` ligne par ligne (panel), calculer aussi `df['ventes_par_m2'] = df['ventes'] / df['n_potentiel']` avant cet export.
  </action>
  <verify>
    <automated>cd /workspaces/formation_causalite && jupyter nbconvert --to notebook --execute formation_causalite.ipynb --output /tmp/test_exec.ipynb 2>&1 | tail -5 && python -c "import pandas as pd; df=pd.read_csv('data/base_panel.csv'); assert 'ventes_par_m2' in df.columns, 'colonne manquante'; print('OK ventes_par_m2 dans CSV')"</automated>
  </verify>
  <done>La colonne ventes_par_m2 est dans base_panel.csv et le notebook s'exécute sans erreur.</done>
</task>

<task type="auto">
  <name>Task 2: Ajouter une figure ventes_par_m2 par taille</name>
  <files>formation_causalite.ipynb</files>
  <action>
Dans la section Scénario 0 (après les figures existantes sc0_distribution, sc0_scatter, sc0_top10, sc0_loi_grands_nombres), ajouter une nouvelle cellule code avec l'id `code-sc0-fig-vm2` :

```python
# Figure — Ventes par m² selon la taille du magasin
fig, ax = plt.subplots(figsize=(7, 4))
tailles_ordre = ['petit', 'moyen', 'grand']
vm2_by_taille = magasins.groupby('taille')['ventes_par_m2'].mean().reindex(tailles_ordre)
bar_colors = [colors_sc0[t] for t in tailles_ordre]
ax.bar(tailles_ordre, vm2_by_taille.values, color=bar_colors, alpha=0.85)
ax.set_xlabel('Taille du magasin')
ax.set_ylabel('Ventes par m² moyen (€)')
ax.set_title('Ventes par m² selon la taille — mesure intensive')
fig.tight_layout()
fig.savefig('figures/sc0_ventes_par_m2.png', dpi=150, bbox_inches='tight')
plt.show()
```

Utiliser `colors_sc0` déjà défini dans `code-sc0-fig1`. Le dict `colors_sc0 = {'petit': '#e74c3c', 'moyen': '#3498db', 'grand': '#2ecc71'}` est disponible dans la même section. S'assurer que cette cellule est placée après la définition de `colors_sc0` et après que `magasins` avec `ventes_par_m2` soit calculé.

Ajouter aussi le chemin `'figures/sc0_ventes_par_m2.png'` à la liste des figures vérifiées dans la cellule de validation finale.
  </action>
  <verify>
    <automated>cd /workspaces/formation_causalite && python -c "import os; assert os.path.exists('figures/sc0_ventes_par_m2.png'), 'figure manquante'; print('OK figure existe')"</automated>
  </verify>
  <done>figures/sc0_ventes_par_m2.png existe et représente ventes_par_m2 par taille (petit/moyen/grand).</done>
</task>

</tasks>

<verification>
Après exécution complète :
1. `data/base_panel.csv` contient la colonne `ventes_par_m2`
2. `figures/sc0_ventes_par_m2.png` existe
3. `jupyter nbconvert --execute` termine sans erreur
</verification>

<success_criteria>
- VM2-01 : ventes_par_m2 calculée et dans le CSV exporté
- VM2-02 : figure sc0_ventes_par_m2.png produite par taille
- Restart & Run All sans erreur
</success_criteria>

<output>
Après completion, créer `.planning/phases/08-ventes-par-m2-et-dag-uniforme/08-01-SUMMARY.md`
</output>
