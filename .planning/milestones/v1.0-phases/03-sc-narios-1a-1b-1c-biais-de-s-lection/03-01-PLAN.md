---
phase: 03-sc-narios-1a-1b-1c-biais-de-s-lection
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - formation_causalite.ipynb
autonomous: true
requirements: [SC1-01, SC1-02]

must_haves:
  truths:
    - "Le scénario 1a affiche un DAG 3 noeuds (Pub, Ventes, Qualité équipe) avec layout fixe, exporté figures/sc1a_dag.png"
    - "Le scénario 1a montre un OLS naïf qui surestime l'ATT — coeff plot et bar chart exportés figures/sc1a_coeff.png et figures/sc1a_bar.png"
    - "Le scénario 1b affiche un DAG 3 noeuds (Pub, Ventes, Localisation) avec layout fixe, exporté figures/sc1b_dag.png"
    - "Le scénario 1b montre un OLS naïf qui surestime l'ATT — coeff plot et bar chart exportés figures/sc1b_coeff.png et figures/sc1b_bar.png"
    - "Deux CSV exportés : data/sc1a_selection_qualite.csv et data/sc1b_selection_urbain.csv"
    - "Chaque scénario utilise un rng local dédié (SEED+10/+11 pour 1a, SEED+20/+21 pour 1b) — rng global non pollué"
  artifacts:
    - path: "figures/sc1a_dag.png"
      provides: "DAG Scénario 1a"
    - path: "figures/sc1a_coeff.png"
      provides: "Coefficient plot Scénario 1a"
    - path: "figures/sc1a_bar.png"
      provides: "Bar chart Scénario 1a"
    - path: "figures/sc1b_dag.png"
      provides: "DAG Scénario 1b"
    - path: "figures/sc1b_coeff.png"
      provides: "Coefficient plot Scénario 1b"
    - path: "figures/sc1b_bar.png"
      provides: "Bar chart Scénario 1b"
    - path: "data/sc1a_selection_qualite.csv"
      provides: "Dataset scénario 1a"
    - path: "data/sc1b_selection_urbain.csv"
      provides: "Dataset scénario 1b"
  key_links:
    - from: "code-sc1a-data"
      to: "compute_outcomes"
      via: "appel direct — deux fois (observé + contrefactuel)"
      pattern: "compute_outcomes\\(df_sc1a"
    - from: "code-sc1a-data"
      to: "smf.ols"
      via: "formule ventes ~ pub + qualite_equipe + C(mois)"
      pattern: "smf\\.ols"
    - from: "code-sc1b-data"
      to: "compute_outcomes"
      via: "appel direct — deux fois (observé + contrefactuel)"
      pattern: "compute_outcomes\\(df_sc1b"
---

<objective>
Insérer les scénarios 1a (sélection par qualité d'équipe) et 1b (sélection par localisation urbain/rural) dans le notebook. Chaque scénario produit 4 cellules de code : assignation pub + ATT + OLS + export CSV, DAG networkx, coefficient plot (horizontal errorbar), bar chart comparaison.

Purpose: Illustrer que quand un confondant détermine l'assignation au traitement (pub), comparer naïvement traités et non-traités surestime l'effet. Scénario 1a confondant = qualite_equipe, scénario 1b confondant = urbain.

Output: 8 PNG (dag + coeff + bar × 2), 2 CSV, 8 nouvelles cellules dans le notebook (1 markdown + 4 code × 2 scénarios, soit md-sc1a + 4 cellules + md-sc1b + 4 cellules).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-sc-narios-1a-1b-1c-biais-de-s-lection/03-CONTEXT.md
@.planning/phases/03-sc-narios-1a-1b-1c-biais-de-s-lection/03-RESEARCH.md

<interfaces>
<!-- Fonctions DGP disponibles dans le notebook (déjà définies) -->

```python
# generate_base_panel — retourne base_df (4800 lignes, 200 magasins × 24 mois)
# Colonnes utiles : magasin_id, taille, urbain, qualite_equipe, mois,
#                   effet_saison_val, ventes, panier_moyen (pub=0 baseline)

# compute_outcomes(df, params, rng) — recalcule ventes et panier_moyen selon df['pub']
# Retourne un nouveau DataFrame (fait .copy() en interne)
# Utilise rng.binomial() et rng.normal() — consomme du rng state

# smf = statsmodels.formula.api (déjà importé)
# nx = networkx (déjà importé)
# base_df = panel partagé disponible en mémoire (cellule code-dgp-exec déjà exécutée)
```

<!-- Paramètres disponibles dans PARAMS (cellule code-params) -->

```python
PARAMS = {
    'P_PUB_BONNE_EQUIPE': 0.70,
    'P_PUB_MAUVAISE_EQUIPE': 0.30,
    'P_PUB_URBAIN': 0.65,
    'P_PUB_RURAL': 0.25,
    # ... autres params
}
# Variables directes (aussi disponibles) :
# SEED = 42
# P_PUB_BONNE_EQUIPE, P_PUB_MAUVAISE_EQUIPE, P_PUB_URBAIN, P_PUB_RURAL
```

<!-- Patterns OLS vérifiés — statsmodels 0.14.6 -->

```python
result = smf.ols('ventes ~ pub + C(mois)', data=df).fit()
coef = result.params['pub']              # float
ci   = result.conf_int()                 # colonnes 0 et 1 (integers, pas 'lower'/'upper')
lower = ci.loc['pub', 0]
upper = ci.loc['pub', 1]
# xerr pour errorbar (distances positives) :
xerr = [[coef - lower], [upper - coef]]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insérer cellules données et DAG pour scénarios 1a et 1b</name>
  <files>formation_causalite.ipynb</files>
  <action>
Insérer 10 cellules après la cellule index 15 (code-dag-pattern, dernière cellule actuelle) :

**Cellule 1 — Markdown section 1a :**
```
## Scénario 1a — Biais de sélection par qualité d'équipe

Le confondant `qualite_equipe` détermine à la fois la probabilité de recevoir la pub et le niveau des ventes. Comparer naïvement magasins traités et non traités surestime l'effet de la pub.
```

**Cellule 2 — code-sc1a-data :**
```python
# ─────────────────────────────────────────────
# Scénario 1a — Sélection par qualité d'équipe
# Confondant : qualite_equipe (binaire)
# Assignation pub : niveau magasin (constant sur 24 mois)
# ─────────────────────────────────────────────

rng_sc1a = np.random.default_rng(SEED + 10)

# Assignation pub au niveau magasin
stores_1a = base_df[['magasin_id', 'qualite_equipe']].drop_duplicates('magasin_id').copy()
probs_1a = np.where(
    stores_1a['qualite_equipe'] == 1,
    P_PUB_BONNE_EQUIPE,
    P_PUB_MAUVAISE_EQUIPE
)
stores_1a['pub'] = rng_sc1a.binomial(1, probs_1a)

# Merge vers le panel complet + calcul des outcomes observés
df_sc1a = base_df.merge(stores_1a[['magasin_id', 'pub']], on='magasin_id')
df_sc1a = compute_outcomes(df_sc1a, PARAMS, rng_sc1a)

# Calcul ATT contrefactuel (rng dédié pour isoler la reproductibilité)
treated_ids_1a = stores_1a[stores_1a['pub'] == 1]['magasin_id'].values
df_treated_1a = df_sc1a[df_sc1a['magasin_id'].isin(treated_ids_1a)].copy()
rng_cf_1a = np.random.default_rng(SEED + 11)
df_cf_1a = df_treated_1a.copy()
df_cf_1a['pub'] = 0
df_cf_1a = compute_outcomes(df_cf_1a, PARAMS, rng_cf_1a)
att_1a = (df_treated_1a['ventes'].values - df_cf_1a['ventes'].values).mean()

# OLS naïf : contrôle la saisonnalité mais PAS le confondant qualite_equipe
model_naive_1a = smf.ols('ventes ~ pub + C(mois)', data=df_sc1a).fit()
# OLS ajusté : contrôle qualite_equipe et la saisonnalité
model_adj_1a   = smf.ols('ventes ~ pub + qualite_equipe + C(mois)', data=df_sc1a).fit()

# Export CSV
df_sc1a.to_csv('data/sc1a_selection_qualite.csv', index=False)
print(f"Sc1a — Traités: {len(treated_ids_1a)}/200 magasins")
print(f"Sc1a — ATT: {att_1a:.1f} €  |  OLS naïf: {model_naive_1a.params['pub']:.1f} €  |  OLS ajusté: {model_adj_1a.params['pub']:.1f} €")
```

**Cellule 3 — code-sc1a-dag :**
```python
# ─────────────────────────────────────────────
# Figure DAG — Scénario 1a
# ─────────────────────────────────────────────

G_1a = nx.DiGraph()
G_1a.add_edges_from([
    ('Qualité équipe', 'Pub'),
    ('Qualité équipe', 'Ventes'),
    ('Pub', 'Ventes'),
])
pos_1a = {'Pub': (0, 0), 'Ventes': (2, 0), 'Qualité équipe': (1, 1)}

color_map_1a = {'Pub': 'steelblue', 'Ventes': 'seagreen', 'Qualité équipe': 'darkorange'}
node_colors_1a = [color_map_1a[n] for n in G_1a.nodes()]

fig, ax = plt.subplots(figsize=(5, 3))
nx.draw_networkx(G_1a, pos_1a, ax=ax,
                 node_color=node_colors_1a,
                 node_size=2000, font_size=9, font_color='white',
                 arrows=True, arrowsize=20)
ax.axis('off')
ax.set_title('DAG — Scénario 1a : sélection par qualité équipe')
fig.savefig('figures/sc1a_dag.png', dpi=150, bbox_inches='tight')
plt.show()
```

**Cellule 4 — Markdown section 1b :**
```
## Scénario 1b — Biais de sélection par localisation

Le confondant `urbain` détermine à la fois la probabilité de recevoir la pub et le niveau des ventes. Comparer naïvement les magasins traités et non traités surestime l'effet de la pub.
```

**Cellule 5 — code-sc1b-data :**
```python
# ─────────────────────────────────────────────
# Scénario 1b — Sélection par localisation urbain/rural
# Confondant : urbain (binaire)
# Assignation pub : niveau magasin (constant sur 24 mois)
# ─────────────────────────────────────────────

rng_sc1b = np.random.default_rng(SEED + 20)

# Assignation pub au niveau magasin
stores_1b = base_df[['magasin_id', 'urbain']].drop_duplicates('magasin_id').copy()
probs_1b = np.where(
    stores_1b['urbain'] == 1,
    P_PUB_URBAIN,
    P_PUB_RURAL
)
stores_1b['pub'] = rng_sc1b.binomial(1, probs_1b)

# Merge vers le panel complet + calcul des outcomes observés
df_sc1b = base_df.merge(stores_1b[['magasin_id', 'pub']], on='magasin_id')
df_sc1b = compute_outcomes(df_sc1b, PARAMS, rng_sc1b)

# Calcul ATT contrefactuel
treated_ids_1b = stores_1b[stores_1b['pub'] == 1]['magasin_id'].values
df_treated_1b = df_sc1b[df_sc1b['magasin_id'].isin(treated_ids_1b)].copy()
rng_cf_1b = np.random.default_rng(SEED + 21)
df_cf_1b = df_treated_1b.copy()
df_cf_1b['pub'] = 0
df_cf_1b = compute_outcomes(df_cf_1b, PARAMS, rng_cf_1b)
att_1b = (df_treated_1b['ventes'].values - df_cf_1b['ventes'].values).mean()

# OLS naïf : contrôle la saisonnalité mais PAS le confondant urbain
model_naive_1b = smf.ols('ventes ~ pub + C(mois)', data=df_sc1b).fit()
# OLS ajusté : contrôle urbain et la saisonnalité
model_adj_1b   = smf.ols('ventes ~ pub + urbain + C(mois)', data=df_sc1b).fit()

# Export CSV
df_sc1b.to_csv('data/sc1b_selection_urbain.csv', index=False)
print(f"Sc1b — Traités: {len(treated_ids_1b)}/200 magasins")
print(f"Sc1b — ATT: {att_1b:.1f} €  |  OLS naïf: {model_naive_1b.params['pub']:.1f} €  |  OLS ajusté: {model_adj_1b.params['pub']:.1f} €")
```

**Cellule 6 — code-sc1b-dag :**
```python
# ─────────────────────────────────────────────
# Figure DAG — Scénario 1b
# ─────────────────────────────────────────────

G_1b = nx.DiGraph()
G_1b.add_edges_from([
    ('Localisation', 'Pub'),
    ('Localisation', 'Ventes'),
    ('Pub', 'Ventes'),
])
pos_1b = {'Pub': (0, 0), 'Ventes': (2, 0), 'Localisation': (1, 1)}

color_map_1b = {'Pub': 'steelblue', 'Ventes': 'seagreen', 'Localisation': 'darkorange'}
node_colors_1b = [color_map_1b[n] for n in G_1b.nodes()]

fig, ax = plt.subplots(figsize=(5, 3))
nx.draw_networkx(G_1b, pos_1b, ax=ax,
                 node_color=node_colors_1b,
                 node_size=2000, font_size=9, font_color='white',
                 arrows=True, arrowsize=20)
ax.axis('off')
ax.set_title('DAG — Scénario 1b : sélection par localisation')
fig.savefig('figures/sc1b_dag.png', dpi=150, bbox_inches='tight')
plt.show()
```

Insérer ces 6 cellules en JSON notebook après la dernière cellule existante (index 15 = code-dag-pattern). Utiliser `json` Python pour lire/modifier/écrire le `.ipynb`.

Anti-patterns CRITIQUES :
- Ne PAS utiliser `rng` global — toujours `rng_sc1a`, `rng_sc1b`, `rng_cf_1a`, `rng_cf_1b`
- `drop_duplicates('magasin_id')` OBLIGATOIRE avant l'assignation niveau magasin
- `node_colors = [color_map[n] for n in G.nodes()]` — jamais une liste littérale
- `fig.savefig(...)` AVANT `plt.show()` — ordre obligatoire
  </action>
  <verify>
    <automated>python -c "
import json
with open('formation_causalite.ipynb') as f:
    nb = json.load(f)
cells = nb['cells']
sources = [''.join(c['source']) for c in cells]
checks = [
    'md-sc1a-section' in str(sources) or 'Scénario 1a' in str(sources),
    'code-sc1a-data' in str(sources) or 'rng_sc1a' in str(sources),
    'code-sc1a-dag' in str(sources) or 'G_1a' in str(sources),
    'md-sc1b-section' in str(sources) or 'Scénario 1b' in str(sources),
    'code-sc1b-data' in str(sources) or 'rng_sc1b' in str(sources),
    'code-sc1b-dag' in str(sources) or 'G_1b' in str(sources),
    len(cells) >= 22,
]
for i, c in enumerate(checks):
    print(f'Check {i+1}: {\"OK\" if c else \"FAIL\"}')
print(f'Total cells: {len(cells)}')
assert all(checks), 'One or more checks failed'
print('PASS')
"
</automated>
  </verify>
  <done>6 cellules insérées (2 markdown + 4 code) pour scénarios 1a et 1b. Notebook contient >= 22 cellules. rng_sc1a et rng_sc1b définis localement avec SEED+10 et SEED+20. CSV exports codés.</done>
</task>

<task type="auto">
  <name>Task 2: Insérer coefficient plots et bar charts pour scénarios 1a et 1b</name>
  <files>formation_causalite.ipynb</files>
  <action>
Insérer 4 cellules supplémentaires (code-sc1a-coeff, code-sc1a-bar, code-sc1b-coeff, code-sc1b-bar) après les cellules du Task 1.

**Cellule 7 — code-sc1a-coeff :**
```python
# ─────────────────────────────────────────────
# Figure coefficient plot — Scénario 1a
# 3 points : OLS naïf, OLS ajusté, ATT (valeur vraie)
# ─────────────────────────────────────────────

coef_naive_1a = model_naive_1a.params['pub']
ci_naive_1a   = model_naive_1a.conf_int()
lower_naive_1a = ci_naive_1a.loc['pub', 0]
upper_naive_1a = ci_naive_1a.loc['pub', 1]

coef_adj_1a = model_adj_1a.params['pub']
ci_adj_1a   = model_adj_1a.conf_int()
lower_adj_1a = ci_adj_1a.loc['pub', 0]
upper_adj_1a = ci_adj_1a.loc['pub', 1]

# xerr = distances positives par rapport au centre (pas les bornes absolues)
xerr_naive_1a = [[coef_naive_1a - lower_naive_1a], [upper_naive_1a - coef_naive_1a]]
xerr_adj_1a   = [[coef_adj_1a   - lower_adj_1a],   [upper_adj_1a   - coef_adj_1a]]

estimators_1a = ['OLS naïf', 'OLS ajusté', 'Valeur vraie (ATT)']
y_pos_1a = [2, 1, 0]

fig, ax = plt.subplots(figsize=(8, 4))
ax.errorbar(coef_naive_1a, 2, xerr=xerr_naive_1a, fmt='o', capsize=5,
            color='#e74c3c', label='OLS naïf', markersize=8)
ax.errorbar(coef_adj_1a, 1, xerr=xerr_adj_1a, fmt='o', capsize=5,
            color='#3498db', label='OLS ajusté', markersize=8)
ax.errorbar(att_1a, 0, fmt='D', color='#2ecc71', label='Valeur vraie (ATT)', markersize=10)
ax.axvline(x=att_1a, color='gray', linestyle='--', alpha=0.5)
ax.set_yticks(y_pos_1a)
ax.set_yticklabels(estimators_1a)
ax.set_xlabel('Effet estimé de la pub sur les ventes (€)')
ax.set_title('Scénario 1a — Coefficients OLS naïf vs ajusté vs valeur vraie')
ax.legend(loc='lower right')
fig.savefig('figures/sc1a_coeff.png', dpi=150, bbox_inches='tight')
plt.show()
```

**Cellule 8 — code-sc1a-bar :**
```python
# ─────────────────────────────────────────────
# Figure bar chart — Scénario 1a
# ─────────────────────────────────────────────

fig, ax = plt.subplots(figsize=(7, 4))
labels_1a = ['OLS naïf', 'OLS ajusté', 'Valeur vraie (ATT)']
values_1a = [coef_naive_1a, coef_adj_1a, att_1a]
colors_est = ['#e74c3c', '#3498db', '#2ecc71']

ax.bar(range(len(labels_1a)), values_1a, color=colors_est, width=0.5)
ax.set_xticks(range(len(labels_1a)))
ax.set_xticklabels(labels_1a)
ax.set_ylabel('Effet estimé de la pub sur les ventes (€)')
ax.set_title('Scénario 1a — Comparaison des estimateurs')
ax.axhline(y=att_1a, color='gray', linestyle='--', alpha=0.5)
fig.savefig('figures/sc1a_bar.png', dpi=150, bbox_inches='tight')
plt.show()
```

**Cellule 9 — code-sc1b-coeff :**
```python
# ─────────────────────────────────────────────
# Figure coefficient plot — Scénario 1b
# ─────────────────────────────────────────────

coef_naive_1b = model_naive_1b.params['pub']
ci_naive_1b   = model_naive_1b.conf_int()
lower_naive_1b = ci_naive_1b.loc['pub', 0]
upper_naive_1b = ci_naive_1b.loc['pub', 1]

coef_adj_1b = model_adj_1b.params['pub']
ci_adj_1b   = model_adj_1b.conf_int()
lower_adj_1b = ci_adj_1b.loc['pub', 0]
upper_adj_1b = ci_adj_1b.loc['pub', 1]

xerr_naive_1b = [[coef_naive_1b - lower_naive_1b], [upper_naive_1b - coef_naive_1b]]
xerr_adj_1b   = [[coef_adj_1b   - lower_adj_1b],   [upper_adj_1b   - coef_adj_1b]]

estimators_1b = ['OLS naïf', 'OLS ajusté', 'Valeur vraie (ATT)']
y_pos_1b = [2, 1, 0]

fig, ax = plt.subplots(figsize=(8, 4))
ax.errorbar(coef_naive_1b, 2, xerr=xerr_naive_1b, fmt='o', capsize=5,
            color='#e74c3c', label='OLS naïf', markersize=8)
ax.errorbar(coef_adj_1b, 1, xerr=xerr_adj_1b, fmt='o', capsize=5,
            color='#3498db', label='OLS ajusté', markersize=8)
ax.errorbar(att_1b, 0, fmt='D', color='#2ecc71', label='Valeur vraie (ATT)', markersize=10)
ax.axvline(x=att_1b, color='gray', linestyle='--', alpha=0.5)
ax.set_yticks(y_pos_1b)
ax.set_yticklabels(estimators_1b)
ax.set_xlabel('Effet estimé de la pub sur les ventes (€)')
ax.set_title('Scénario 1b — Coefficients OLS naïf vs ajusté vs valeur vraie')
ax.legend(loc='lower right')
fig.savefig('figures/sc1b_coeff.png', dpi=150, bbox_inches='tight')
plt.show()
```

**Cellule 10 — code-sc1b-bar :**
```python
# ─────────────────────────────────────────────
# Figure bar chart — Scénario 1b
# ─────────────────────────────────────────────

fig, ax = plt.subplots(figsize=(7, 4))
labels_1b = ['OLS naïf', 'OLS ajusté', 'Valeur vraie (ATT)']
values_1b = [coef_naive_1b, coef_adj_1b, att_1b]

ax.bar(range(len(labels_1b)), values_1b, color=colors_est, width=0.5)
ax.set_xticks(range(len(labels_1b)))
ax.set_xticklabels(labels_1b)
ax.set_ylabel('Effet estimé de la pub sur les ventes (€)')
ax.set_title('Scénario 1b — Comparaison des estimateurs')
ax.axhline(y=att_1b, color='gray', linestyle='--', alpha=0.5)
fig.savefig('figures/sc1b_bar.png', dpi=150, bbox_inches='tight')
plt.show()
```

Note : `colors_est` est défini dans code-sc1a-bar — si les deux cellules sont dans la même session notebook, elle est disponible. Si besoin de robustesse, redéfinir `colors_est = ['#e74c3c', '#3498db', '#2ecc71']` dans code-sc1b-coeff.

Anti-patterns :
- `ci.loc['pub', 0]` et `ci.loc['pub', 1]` — PAS `['lower']`/`['upper']`
- `xerr` = distances positives — `[[coef - lower], [upper - coef]]`
- `fig.savefig(...)` AVANT `plt.show()`
  </action>
  <verify>
    <automated>python -c "
import json, os
with open('formation_causalite.ipynb') as f:
    nb = json.load(f)
sources = [''.join(c['source']) for c in nb['cells']]
all_src = '\n'.join(sources)
checks = {
    'sc1a_coeff code present': 'sc1a_coeff' in all_src or 'coef_naive_1a' in all_src,
    'sc1a_bar code present': 'sc1a_bar' in all_src or 'values_1a' in all_src,
    'sc1b_coeff code present': 'sc1b_coeff' in all_src or 'coef_naive_1b' in all_src,
    'sc1b_bar code present': 'sc1b_bar' in all_src or 'values_1b' in all_src,
    'errorbar present': 'errorbar' in all_src,
    'conf_int present': 'conf_int' in all_src,
    'correct ci access': 'loc[' in all_src and ('0]' in all_src or \"0]\\'\" in all_src),
    'savefig before show': all_src.index('sc1a_coeff.png') < all_src.index('sc1a_coeff') + 500,
    'nb >= 26 cells': len(nb['cells']) >= 26,
}
for k, v in checks.items():
    print(f'{\"OK\" if v else \"FAIL\"}: {k}')
print(f'Total cells: {len(nb[\"cells\"])}')
assert all(checks.values()), 'One or more checks failed'
print('PASS')
"
</automated>
  </verify>
  <done>4 cellules coeff plot et bar chart insérées pour 1a et 1b. Notebook >= 26 cellules. Tous les appels errorbar présents avec xerr distances positives. conf_int() accédé via .loc['pub', 0] et .loc['pub', 1]. savefig avant show dans toutes les cellules.</done>
</task>

</tasks>

<verification>
Vérifications globales du plan 03-01 :

1. `jupyter nbconvert --to notebook --execute formation_causalite.ipynb --output /tmp/test_03_01.ipynb --ExecutePreprocessor.timeout=300` — exit code 0 requis
2. `ls figures/sc1a_dag.png figures/sc1a_coeff.png figures/sc1a_bar.png figures/sc1b_dag.png figures/sc1b_coeff.png figures/sc1b_bar.png` — 6 fichiers présents
3. `ls data/sc1a_selection_qualite.csv data/sc1b_selection_urbain.csv` — 2 CSV présents
4. Print output dans le notebook montre ATT < OLS naïf pour 1a (surestimation pédagogiquement visible)
</verification>

<success_criteria>
- 8 nouvelles cellules dans le notebook (2 markdown + 6 code : data×2, dag×2, coeff×2, bar×2)
- 6 PNG exportés dans figures/ (dag, coeff, bar pour chaque scénario)
- 2 CSV exportés dans data/
- ATT < OLS naïf pour les deux scénarios (biais dans la bonne direction)
- OLS naïf utilise `C(mois)` mais PAS le confondant ; OLS ajusté inclut le confondant
- rng global non pollué (seeds dédiés SEED+10/+11 pour 1a, SEED+20/+21 pour 1b)
- nbconvert exit 0
</success_criteria>

<output>
After completion, create `.planning/phases/03-sc-narios-1a-1b-1c-biais-de-s-lection/03-01-SUMMARY.md`
</output>
