---
phase: 06-validation-bout-en-bout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - formation_causalite.ipynb
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - INFRA-04

must_haves:
  truths:
    - "Le notebook s'exécute de bout en bout (Restart & Run All) sans erreur — nbconvert exit 0"
    - "Aucun magic number de simulation hors cellule Paramètres — seul P_PUB_ALEATOIRE remplace 0.5 dans sc2 et sc3"
    - "Les 20 PNG et 7 CSV sont confirmés présents par une cellule de validation en fin de notebook"
    - "La cohérence de EFFET_PUB_VISITES et EFFET_PUB_PANIER entre scénarios est assertée explicitement"
  artifacts:
    - path: "formation_causalite.ipynb"
      provides: "Notebook final avec 32 cellules — paramètre P_PUB_ALEATOIRE + cellule validation"
      contains: "P_PUB_ALEATOIRE"
  key_links:
    - from: "In[1] — PARAMS"
      to: "In[24] et In[28]"
      via: "P_PUB_ALEATOIRE variable"
      pattern: "P_PUB_ALEATOIRE"
    - from: "In[32] — validation finale"
      to: "figures/ et data/"
      via: "Path(f).exists() assertions"
      pattern: "png_manquants|csv_manquants"
---

<objective>
Finaliser le notebook pour distribution : ajouter le paramètre `P_PUB_ALEATOIRE` dans la cellule Paramètres (supprime le seul magic number restant), puis ajouter une cellule de validation finale qui confirme la présence de tous les fichiers générés et la cohérence des effets vrais. Valider l'ensemble par `nbconvert --execute` (exit 0).

Purpose: Le formateur peut distribuer le notebook avec la garantie que `Restart & Run All` fonctionne et que tous les artefacts sont produits.
Output: Notebook 32 cellules propre, reproductible, validé bout-en-bout.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-validation-bout-en-bout/06-CONTEXT.md
@.planning/phases/06-validation-bout-en-bout/06-RESEARCH.md
</context>

<interfaces>
<!-- État actuel du notebook (31 cellules, nbconvert exit 0 déjà validé en Phase 5) -->

<!-- In[1] — Cellule PARAMS (extrait) -->
```python
# Variables ALL_CAPS → dict PARAMS
P_BASE_VISITE = 0.25
P_PUB_BASSE_SAISON = 0.40   # exemple
# ... autres constantes ...
PARAMS = {
    'p_base_visite': P_BASE_VISITE,
    # ... autres entrées ...
}
```

<!-- In[24] — sc2 : assignation pub (AVANT correction) -->
```python
stores_sc2['pub'] = rng_sc2.binomial(1, 0.5, size=len(stores_sc2))
```

<!-- In[28] — sc3 : assignation pub (AVANT correction) -->
```python
stores_sc3['pub'] = rng_sc3.binomial(1, 0.5, size=len(stores_sc3))
```

<!-- Fichiers PNG attendus : 20 fichiers dans figures/ -->
dag_pattern_demo.png, sc0_distribution.png, sc0_loi_grands_nombres.png,
sc0_scatter.png, sc0_top10.png, sc1a_bar.png, sc1a_coeff.png, sc1a_dag.png,
sc1b_bar.png, sc1b_coeff.png, sc1b_dag.png, sc1c_bar.png, sc1c_coeff.png,
sc1c_dag.png, sc2_bar.png, sc2_coeff.png, sc2_dag.png,
sc3_bar.png, sc3_coeff.png, sc3_dag.png

<!-- Fichiers CSV attendus : 7 fichiers dans data/ -->
base_panel.csv, sc0_biais_petits_nombres.csv, sc1a_selection_qualite.csv,
sc1b_selection_urbain.csv, sc1c_selection_saison.csv, sc2_mediateur.csv, sc3_collider.csv
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Ajouter P_PUB_ALEATOIRE dans PARAMS et corriger sc2/sc3</name>
  <files>formation_causalite.ipynb</files>
  <action>
Modifier le notebook JSON pour effectuer trois changements dans les cellules existantes :

**Changement A — Cellule In[1] (cellule PARAMS) :**
Dans la section des constantes ALL_CAPS, ajouter après les lignes de probabilité pub (par exemple après P_PUB_BASSE_SAISON) :
```python
P_PUB_ALEATOIRE = 0.5    # sc2, sc3 : assignation pub aléatoire (pas de confondant)
```
Dans le dict PARAMS, ajouter l'entrée correspondante :
```python
'p_pub_aleatoire': P_PUB_ALEATOIRE,
```
Placer cette entrée dans PARAMS après les autres paramètres de probabilité pub (avant les clés collider si elles existent).

**Changement B — Cellule In[24] (sc2 données) :**
Remplacer la ligne :
```python
stores_sc2['pub'] = rng_sc2.binomial(1, 0.5, size=len(stores_sc2))
```
par :
```python
stores_sc2['pub'] = rng_sc2.binomial(1, P_PUB_ALEATOIRE, size=len(stores_sc2))
```

**Changement C — Cellule In[28] (sc3 données) :**
Remplacer la ligne :
```python
stores_sc3['pub'] = rng_sc3.binomial(1, 0.5, size=len(stores_sc3))
```
par :
```python
stores_sc3['pub'] = rng_sc3.binomial(1, P_PUB_ALEATOIRE, size=len(stores_sc3))
```

Pour modifier le notebook, utiliser l'approche Python json :
```python
import json
with open('formation_causalite.ipynb', 'r') as f:
    nb = json.load(f)
# Modifier nb['cells'][i]['source'] pour les cellules concernées
# Identifier les cellules par leur contenu (chercher P_BASE_VISITE pour In[1],
# 'rng_sc2.binomial' pour In[24], 'rng_sc3.binomial' pour In[28])
with open('formation_causalite.ipynb', 'w') as f:
    json.dump(nb, f, ensure_ascii=False, indent=1)
```

Vérifier après modification que `'p_pub_aleatoire'` apparaît bien dans la cellule PARAMS et que `0.5` n'apparaît plus comme littéral dans les cellules sc2 et sc3 (grep de contrôle).
  </action>
  <verify>
```bash
python -c "
import json
with open('/workspaces/formation_causalite/formation_causalite.ipynb') as f:
    nb = json.load(f)
src = ' '.join([''.join(c['source']) for c in nb['cells']])
assert 'P_PUB_ALEATOIRE = 0.5' in src, 'Constante P_PUB_ALEATOIRE manquante'
assert \"'p_pub_aleatoire': P_PUB_ALEATOIRE\" in src, 'Entrée PARAMS p_pub_aleatoire manquante'
assert 'rng_sc2.binomial(1, P_PUB_ALEATOIRE' in src, 'sc2 non corrigé'
assert 'rng_sc3.binomial(1, P_PUB_ALEATOIRE' in src, 'sc3 non corrigé'
print('OK — P_PUB_ALEATOIRE présent et 0.5 remplacé dans sc2/sc3')
"
```
  </verify>
  <done>La cellule PARAMS (In[1]) contient `P_PUB_ALEATOIRE = 0.5` et `'p_pub_aleatoire': P_PUB_ALEATOIRE` dans le dict PARAMS. Les cellules sc2 (In[24]) et sc3 (In[28]) utilisent `P_PUB_ALEATOIRE` au lieu du littéral `0.5`.</done>
</task>

<task type="auto">
  <name>Task 2: Ajouter la cellule de validation finale (In[32])</name>
  <files>formation_causalite.ipynb</files>
  <action>
Ajouter une nouvelle cellule de code à la fin du notebook (après l'actuelle dernière cellule sc3 bar chart — In[31]).

Contenu exact de la nouvelle cellule :
```python
# ═══════════════════════════════════════════════════════════
# VALIDATION FINALE — Notebook bout-en-bout
# ═══════════════════════════════════════════════════════════

from pathlib import Path

# 1. Fichiers PNG attendus (20 fichiers)
PNG_ATTENDUS = [
    'figures/dag_pattern_demo.png',
    'figures/sc0_distribution.png',
    'figures/sc0_loi_grands_nombres.png',
    'figures/sc0_scatter.png',
    'figures/sc0_top10.png',
    'figures/sc1a_bar.png',
    'figures/sc1a_coeff.png',
    'figures/sc1a_dag.png',
    'figures/sc1b_bar.png',
    'figures/sc1b_coeff.png',
    'figures/sc1b_dag.png',
    'figures/sc1c_bar.png',
    'figures/sc1c_coeff.png',
    'figures/sc1c_dag.png',
    'figures/sc2_bar.png',
    'figures/sc2_coeff.png',
    'figures/sc2_dag.png',
    'figures/sc3_bar.png',
    'figures/sc3_coeff.png',
    'figures/sc3_dag.png',
]

# 2. Fichiers CSV attendus (7 fichiers)
CSV_ATTENDUS = [
    'data/base_panel.csv',
    'data/sc0_biais_petits_nombres.csv',
    'data/sc1a_selection_qualite.csv',
    'data/sc1b_selection_urbain.csv',
    'data/sc1c_selection_saison.csv',
    'data/sc2_mediateur.csv',
    'data/sc3_collider.csv',
]

# Vérification présence des PNG
png_manquants = [f for f in PNG_ATTENDUS if not Path(f).exists()]
assert not png_manquants, f"PNG manquants : {png_manquants}"

# Vérification présence des CSV
csv_manquants = [f for f in CSV_ATTENDUS if not Path(f).exists()]
assert not csv_manquants, f"CSV manquants : {csv_manquants}"

# 3. Cohérence des effets vrais entre scénarios
assert PARAMS['effet_pub_visites'] == EFFET_PUB_VISITES, \
    f"Incohérence effet_pub_visites : PARAMS={PARAMS['effet_pub_visites']} ≠ {EFFET_PUB_VISITES}"
assert PARAMS['effet_pub_panier'] == EFFET_PUB_PANIER, \
    f"Incohérence effet_pub_panier : PARAMS={PARAMS['effet_pub_panier']} ≠ {EFFET_PUB_PANIER}"

print("✓ Validation notebook : OK")
print(f"  {len(PNG_ATTENDUS)} PNG présents dans figures/")
print(f"  {len(CSV_ATTENDUS)} CSV présents dans data/")
print(f"  Effet pub visites : {EFFET_PUB_VISITES:.0%} (cohérent)")
print(f"  Effet pub panier  : {EFFET_PUB_PANIER:.0%} (cohérent)")
```

Pour ajouter la cellule, utiliser Python json :
```python
import json
with open('formation_causalite.ipynb', 'r') as f:
    nb = json.load(f)
new_cell = {
    "cell_type": "code",
    "execution_count": None,
    "metadata": {},
    "outputs": [],
    "source": [<contenu ci-dessus comme liste de lignes>]
}
nb['cells'].append(new_cell)
with open('formation_causalite.ipynb', 'w') as f:
    json.dump(nb, f, ensure_ascii=False, indent=1)
```

Le notebook doit avoir 32 cellules après cette modification.
  </action>
  <verify>
```bash
python -c "
import json
with open('/workspaces/formation_causalite/formation_causalite.ipynb') as f:
    nb = json.load(f)
n = len(nb['cells'])
last_src = ''.join(nb['cells'][-1]['source'])
assert 'PNG_ATTENDUS' in last_src, 'Cellule de validation absente'
assert 'CSV_ATTENDUS' in last_src, 'CSV_ATTENDUS absent'
assert 'Validation notebook' in last_src, 'Message de validation absent'
assert n == 32, f'Nombre de cellules attendu 32, obtenu {n}'
print(f'OK — {n} cellules, cellule validation présente en dernière position')
"
```
  </verify>
  <done>Le notebook contient 32 cellules. La dernière cellule vérifie 20 PNG + 7 CSV + cohérence EFFET_PUB_VISITES/EFFET_PUB_PANIER et affiche "Validation notebook : OK".</done>
</task>

<task type="auto">
  <name>Task 3: Validation bout-en-bout via nbconvert</name>
  <files>formation_causalite.ipynb</files>
  <action>
Exécuter le notebook complet via nbconvert pour simuler un `Restart & Run All`. Le notebook exécuté est écrit dans `/tmp/` pour ne pas écraser le source.

Commande :
```bash
cd /workspaces/formation_causalite && \
jupyter nbconvert --to notebook --execute formation_causalite.ipynb \
    --output /tmp/formation_causalite_validated.ipynb \
    --ExecutePreprocessor.timeout=300
```

Si exit code != 0 : lire la sortie d'erreur pour identifier la cellule fautive, corriger dans `formation_causalite.ipynb`, et relancer.

Après succès (exit 0), vérifier que le output de la dernière cellule du notebook exécuté contient "Validation notebook : OK" :
```bash
python -c "
import json
with open('/tmp/formation_causalite_validated.ipynb') as f:
    nb = json.load(f)
last_outputs = nb['cells'][-1]['outputs']
output_text = ''.join([o.get('text','') for o in last_outputs if o.get('output_type') in ('stream','execute_result')])
assert 'Validation notebook : OK' in output_text, f'Message OK absent. Outputs: {last_outputs}'
print('SUCCES — nbconvert exit 0, validation notebook OK imprimé')
print(output_text)
"
```
  </action>
  <verify>
```bash
cd /workspaces/formation_causalite && jupyter nbconvert --to notebook --execute formation_causalite.ipynb --output /tmp/formation_causalite_validated.ipynb --ExecutePreprocessor.timeout=300 && echo "Exit: $?"
```
  </verify>
  <done>nbconvert retourne exit 0. Le notebook exécuté contient "Validation notebook : OK" dans l'output de la dernière cellule. Les 20 PNG et 7 CSV sont présents dans figures/ et data/.</done>
</task>

</tasks>

<verification>
1. `formation_causalite.ipynb` contient exactement 32 cellules
2. `P_PUB_ALEATOIRE = 0.5` dans la cellule PARAMS et `'p_pub_aleatoire': P_PUB_ALEATOIRE` dans le dict PARAMS
3. Les cellules sc2 et sc3 utilisent `P_PUB_ALEATOIRE` — plus de `0.5` littéral dans les assignations pub
4. La dernière cellule (In[32]) liste 20 PNG et 7 CSV avec assertions + message de succès
5. `nbconvert --execute` retourne exit 0 sur le notebook complet
6. L'output de la dernière cellule contient "Validation notebook : OK"
</verification>

<success_criteria>
- `Restart & Run All` simule sans erreur (nbconvert exit 0)
- Aucun magic number de simulation hors cellule PARAMS — `P_PUB_ALEATOIRE` remplace les deux occurrences de `0.5`
- Cellule de validation finale imprime "Validation notebook : OK" avec décompte 20 PNG / 7 CSV
- Cohérence `EFFET_PUB_VISITES` et `EFFET_PUB_PANIER` assertée explicitement
- Notebook prêt à distribuer au formateur
</success_criteria>

<output>
After completion, create `.planning/phases/06-validation-bout-en-bout/06-01-SUMMARY.md`
</output>
