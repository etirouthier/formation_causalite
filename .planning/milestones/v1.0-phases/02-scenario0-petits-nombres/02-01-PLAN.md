---
phase: 02-scenario0-petits-nombres
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - formation_causalite.ipynb
  - data/sc0_biais_petits_nombres.csv
autonomous: true
requirements:
  - SC0-01
  - SC0-02

must_haves:
  truths:
    - "La cellule d'agrégation produit agg_sc0 (200 lignes, une par magasin) avec colonnes n_potentiel, nb_visites_moy, panier_moyen_moy, panier_moyen_std"
    - "data/sc0_biais_petits_nombres.csv existe et contient 200 lignes après exécution"
    - "La figure sc0_distribution.png affiche 3 histogrammes empilés verticalement avec axe X partagé, un par taille (petit/moyen/grand)"
    - "La figure sc0_scatter.png affiche un scatter plot agrégé par magasin (200 points) avec corrélation négative visible nb_visites ↔ variance du panier_moyen"
    - "savefig précède plt.show() dans toutes les nouvelles cellules"
  artifacts:
    - path: "formation_causalite.ipynb"
      provides: "Cellules sc0 : markdown section, agrégation+CSV, Figure 1, Figure 2"
      contains: "agg_sc0, sc0_distribution.png, sc0_scatter.png"
    - path: "data/sc0_biais_petits_nombres.csv"
      provides: "Export CSV du scénario 0 (200 magasins agrégés)"
      min_lines: 200
    - path: "figures/sc0_distribution.png"
      provides: "Figure 1 — 3 histogrammes distribution panier_moyen par taille"
  key_links:
    - from: "base_df (4800 lignes)"
      to: "agg_sc0 (200 lignes)"
      via: "groupby(['magasin_id','taille']).agg(...)"
      pattern: "groupby.*magasin_id.*taille"
    - from: "agg_sc0"
      to: "figures/sc0_distribution.png"
      via: "sns.histplot sur subset par taille, subplots(3,1,sharex=True)"
      pattern: "sharex=True"
    - from: "agg_sc0"
      to: "figures/sc0_scatter.png"
      via: "ax.scatter par groupe taille, corrélation nb_visites_moy ↔ panier_moyen_std"
      pattern: "scatter.*nb_visites_moy.*panier_moyen_std"
---

<objective>
Insérer dans le notebook la section Scénario 0, la cellule d'agrégation (agg_sc0 + export CSV), et les Figures 1 et 2 (distribution par taille et scatter variance).

Purpose: Établir la fondation de données (agg_sc0) réutilisée par toutes les figures du scénario, et produire les deux premières figures pédagogiques montrant la distribution du panier_moyen par taille et la corrélation nb_visites ↔ variance.

Output: 4 nouvelles cellules dans le notebook (markdown section + agrégation + fig1 + fig2), export CSV data/sc0_biais_petits_nombres.csv, exports PNG figures/sc0_distribution.png et figures/sc0_scatter.png.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-scenario0-petits-nombres/02-CONTEXT.md
@.planning/phases/01-fondations/01-02-SUMMARY.md

**Contrainte absolue :** Livrable = cellules .ipynb. Toutes les tâches modifient formation_causalite.ipynb en ajoutant des cellules au JSON. Les 4 cellules s'insèrent après la cellule d'id `code-assertions` (index 7 dans le notebook actuel à 10 cellules — vérifier au moment de l'exécution).

**Décisions locked (02-CONTEXT.md) :**
- Figure 1 : 3 histogrammes empilés verticalement avec axe X partagé — `plt.subplots(3, 1, sharex=True)` — données : `panier_moyen_moy` (moyenne sur 24 mois par magasin)
- Figure 2 : scatter agrégé par magasin — axe X : `nb_visites_moy`, axe Y : `panier_moyen_std`
- Export CSV : `data/sc0_biais_petits_nombres.csv` — à minima le DataFrame agrégé par magasin

**Patterns établis en Phase 1 (à respecter) :**
- `fig.savefig('figures/xxx.png', dpi=150, bbox_inches='tight')` AVANT `plt.show()`
- Aucun magic number hors cellule Paramètres — utiliser `PARAMS`, `N_MAGASINS`, `SEED`, etc.
- `rng` global NE DOIT PAS être utilisé pour les simulations de la Figure 4 (plan 02-02) — ici pas de simulation, pas de risque

<interfaces>
<!-- Contrats DGP établis en Phase 1 — l'exécuteur réutilise directement -->

**base_df — disponible en mémoire après cellule code-dgp-exec :**
```
Colonnes : ['magasin_id', 'taille', 'urbain', 'qualite_equipe', 'n_potentiel', 'mois',
            'effet_saison_val', 'p_visite', 'nb_visites', 'ventes', 'panier_moyen']
4800 lignes (200 magasins × 24 mois), pub absent
```

**Agrégation cible agg_sc0 (vérifié empiriquement SEED=42) :**
```python
agg_sc0 = base_df.groupby(['magasin_id', 'taille']).agg(
    n_potentiel=('n_potentiel', 'first'),
    nb_visites_moy=('nb_visites', 'mean'),
    panier_moyen_moy=('panier_moyen', 'mean'),
    panier_moyen_std=('panier_moyen', 'std'),
).reset_index()
# Résultat empirique SEED=42 : 200 lignes
# petit=79, moyen=86, grand=35
# panier_moyen_std : petit≈1.26, moyen≈0.46, grand≈0.26
# corrélation nb_visites_moy / panier_moyen_std : -0.758
```

**Paramètres disponibles (cellule Paramètres ALL_CAPS) :**
```python
SEED = 42
N_MAGASINS = 200
MU_PANIER_BASE = 50.0    # centre attendu des distributions
SIGMA_PANIER = 15.0
N_PETIT = 30; N_MOYEN = 150; N_GRAND = 500
```

**Fonctions DGP disponibles en mémoire :**
```python
generate_base_panel(params, rng)  # → DataFrame caractéristiques
compute_outcomes(df, params, rng)  # → df avec p_visite, nb_visites, ventes, panier_moyen
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insérer cellule markdown de section et cellule d'agrégation + export CSV</name>
  <files>formation_causalite.ipynb</files>
  <action>
Charger formation_causalite.ipynb (JSON). Trouver l'index de la cellule avec `id="code-assertions"` (actuellement index 7 dans un notebook à 10 cellules — toujours vérifier dynamiquement). Insérer deux nouvelles cellules APRÈS cette cellule :

**Cellule A — Markdown (séparateur de section) :**
```markdown
---
## Scénario 0 — Biais de petits nombres

Les petits magasins dominent les extrêmes de distribution du `panier_moyen` en raison de la variance binomiale : avec peu de clients potentiels, la loi des grands nombres n'a pas le temps de lisser les fluctuations.
```
Attribuer l'id `md-sc0-section`.

**Cellule B — Code (agrégation + export CSV) :**
```python
# ─────────────────────────────────────────────
# Scénario 0 — Agrégation par magasin
# ─────────────────────────────────────────────
# Agrégation base_df → 200 points (un par magasin)
# 'taille' dans le groupby est obligatoire pour conserver la colonne dans l'agrégat
agg_sc0 = base_df.groupby(['magasin_id', 'taille']).agg(
    n_potentiel=('n_potentiel', 'first'),
    nb_visites_moy=('nb_visites', 'mean'),
    panier_moyen_moy=('panier_moyen', 'mean'),
    panier_moyen_std=('panier_moyen', 'std'),
).reset_index()

# Export CSV scénario 0
agg_sc0.to_csv('data/sc0_biais_petits_nombres.csv', index=False)
print(f"Scénario 0 : {len(agg_sc0)} magasins agrégés")
print(agg_sc0.groupby('taille').size().to_string())
```
Attribuer l'id `code-sc0-aggregation`.

Points critiques :
- `groupby(['magasin_id', 'taille'])` — les deux colonnes sont requises pour conserver `taille` dans l'agrégat
- `'first'` pour `n_potentiel` est correct car n_potentiel est fixe par magasin sur 24 mois
- `'std'` pour `panier_moyen_std` calcule la std sur 24 mois par magasin — c'est la mesure de variance pour Figure 2
- Pas de magic number — aucun nombre littéral dans ce code
  </action>
  <verify>
    <automated>python3 -c "
import json
nb = json.load(open('formation_causalite.ipynb'))
cells = nb['cells']
ids = [c.get('id','') for c in cells]
assert 'md-sc0-section' in ids, 'md-sc0-section manquant'
assert 'code-sc0-aggregation' in ids, 'code-sc0-aggregation manquant'
all_code = ' '.join(''.join(c['source']) for c in cells if c['cell_type']=='code')
assert 'agg_sc0' in all_code, 'agg_sc0 manquant'
assert \"groupby(['magasin_id', 'taille'])\" in all_code or \"groupby([\\\"magasin_id\\\", \\\"taille\\\"])\" in all_code, 'groupby sur les deux colonnes manquant'
assert 'sc0_biais_petits_nombres.csv' in all_code, 'export CSV manquant'
print(f'OK — {len(cells)} cellules, agrégation sc0 presente')
"</automated>
  </verify>
  <done>Les cellules md-sc0-section et code-sc0-aggregation sont insérées après code-assertions. agg_sc0 est défini avec groupby(['magasin_id','taille']). Export CSV data/sc0_biais_petits_nombres.csv présent dans le code.</done>
</task>

<task type="auto">
  <name>Task 2: Insérer Figure 1 (distribution par taille) et Figure 2 (scatter variance)</name>
  <files>formation_causalite.ipynb</files>
  <action>
Charger formation_causalite.ipynb (JSON). Trouver la cellule `code-sc0-aggregation` et insérer deux nouvelles cellules code APRÈS :

**Cellule C — Code (Figure 1 : distribution par taille) :**
```python
# ─────────────────────────────────────────────
# Figure 1 — Distribution du panier moyen par taille de magasin
# ─────────────────────────────────────────────
colors_sc0 = {'petit': '#e74c3c', 'moyen': '#3498db', 'grand': '#2ecc71'}
tailles_ordre = ['petit', 'moyen', 'grand']

fig, axes = plt.subplots(3, 1, figsize=(9, 9), sharex=True)
for ax, taille in zip(axes, tailles_ordre):
    subset = agg_sc0[agg_sc0['taille'] == taille]
    sns.histplot(data=subset, x='panier_moyen_moy', ax=ax,
                 color=colors_sc0[taille], bins=20)
    ax.set_title(f'Magasins {taille} (N={len(subset)})', fontsize=11)
    ax.set_xlabel('')
    ax.set_ylabel('Nombre de magasins')

axes[-1].set_xlabel('panier_moyen moyen sur 24 mois (€)')
fig.suptitle('Distribution du panier moyen par taille de magasin', fontsize=13)
plt.tight_layout()
fig.savefig('figures/sc0_distribution.png', dpi=150, bbox_inches='tight')
plt.show()
```
Attribuer l'id `code-sc0-fig1`.

**Cellule D — Code (Figure 2 : scatter variance) :**
```python
# ─────────────────────────────────────────────
# Figure 2 — Variance du panier moyen vs affluence
# ─────────────────────────────────────────────
fig, ax = plt.subplots(figsize=(8, 5))
for taille, grp in agg_sc0.groupby('taille'):
    ax.scatter(grp['nb_visites_moy'], grp['panier_moyen_std'],
               color=colors_sc0[taille], label=taille, alpha=0.7, s=40)

ax.set_xlabel('Nombre de visites moyen (24 mois)')
ax.set_ylabel('Écart-type du panier moyen (24 mois)')
ax.set_title('Variance du panier moyen vs affluence par magasin\n'
             '(corrélation négative : plus de clients = moins de variance)')
ax.legend(title='Taille')
fig.savefig('figures/sc0_scatter.png', dpi=150, bbox_inches='tight')
plt.show()
```
Attribuer l'id `code-sc0-fig2`.

Points critiques :
- `colors_sc0` défini UNE SEULE FOIS dans Figure 1 et réutilisé dans Figure 2 (et dans Figure 3/4 du plan 02-02) — ne pas le redéfinir
- `sharex=True` dans `subplots(3, 1, ...)` est OBLIGATOIRE (décision locked)
- `fig.savefig(...)` AVANT `plt.show()` — ordre obligatoire (pattern Phase 1)
- `bins=20` dans sns.histplot : choix de discrétion, acceptable
- `alpha=0.7, s=40` dans scatter : choix de discrétion, acceptable
- Aucun magic number de paramètre — les noms de colonnes font référence aux données, pas à des constantes
  </action>
  <verify>
    <automated>python3 -c "
import json
nb = json.load(open('formation_causalite.ipynb'))
cells = nb['cells']
ids = [c.get('id','') for c in cells]
assert 'code-sc0-fig1' in ids, 'code-sc0-fig1 manquant'
assert 'code-sc0-fig2' in ids, 'code-sc0-fig2 manquant'
all_code = ' '.join(''.join(c['source']) for c in cells if c['cell_type']=='code')
assert 'sharex=True' in all_code, 'sharex=True manquant (decision locked fig1)'
assert 'sc0_distribution.png' in all_code, 'export sc0_distribution.png manquant'
assert 'sc0_scatter.png' in all_code, 'export sc0_scatter.png manquant'
assert 'colors_sc0' in all_code, 'colors_sc0 manquant'
# Vérifier ordre savefig avant show dans chaque cellule
for c in cells:
    if c['cell_type'] == 'code':
        src = ''.join(c['source'])
        if 'savefig' in src and 'plt.show' in src:
            assert src.index('savefig') < src.index('plt.show'), \
                f'savefig APRES plt.show dans cellule {c.get(\"id\",\"?\")} — PNG blanc'
print(f'OK — {len(cells)} cellules, figures 1 et 2 presentes, ordre savefig correct')
"</automated>
  </verify>
  <done>
code-sc0-fig1 produit figures/sc0_distribution.png (3 histogrammes empilés, sharex=True). code-sc0-fig2 produit figures/sc0_scatter.png (scatter 200 points colorés par taille). savefig précède plt.show dans les deux cellules. colors_sc0 défini une fois et partagé.
  </done>
</task>

</tasks>

<verification>
Critères objectifs de complétion du plan 02-01 :

```bash
# 1. Cellules présentes avec bons IDs
python3 -c "
import json
nb = json.load(open('formation_causalite.ipynb'))
ids = [c.get('id','') for c in nb['cells']]
for cid in ['md-sc0-section','code-sc0-aggregation','code-sc0-fig1','code-sc0-fig2']:
    assert cid in ids, f'{cid} manquant'
print(f'OK — {len(nb[\"cells\"])} cellules, 4 nouvelles cellules sc0 presentes')
"

# 2. Exécution notebook sans erreur
jupyter nbconvert --to notebook --execute formation_causalite.ipynb --output /tmp/test_02_01.ipynb 2>&1 | tail -5

# 3. Exports créés
ls -la data/sc0_biais_petits_nombres.csv figures/sc0_distribution.png figures/sc0_scatter.png

# 4. CSV contient 200 lignes de données (+ 1 header)
python3 -c "
import pandas as pd
df = pd.read_csv('data/sc0_biais_petits_nombres.csv')
assert len(df) == 200, f'Attendu 200 lignes, obtenu {len(df)}'
assert 'taille' in df.columns, 'colonne taille manquante'
assert 'panier_moyen_std' in df.columns, 'colonne panier_moyen_std manquante'
print(f'OK — {len(df)} magasins, colonnes: {list(df.columns)}')
"
```
</verification>

<success_criteria>
1. Le notebook s'exécute sans erreur via `jupyter nbconvert --execute` (exit code 0)
2. agg_sc0 contient 200 lignes (une par magasin) avec colonnes taille, nb_visites_moy, panier_moyen_moy, panier_moyen_std
3. data/sc0_biais_petits_nombres.csv exporté avec 200 lignes
4. figures/sc0_distribution.png : 3 histogrammes empilés avec sharex=True, axe X partagé
5. figures/sc0_scatter.png : scatter 200 points colorés par taille (petit/moyen/grand)
6. savefig précède plt.show dans toutes les nouvelles cellules
</success_criteria>

<output>
Après complétion, créer `.planning/phases/02-scenario0-petits-nombres/02-01-SUMMARY.md` avec :
- Nombre de cellules dans le notebook après ce plan
- IDs des cellules ajoutées
- Résultat empirique de l'agrégation agg_sc0 (tailles des groupes, corrélation observée)
- Toute déviation par rapport au plan et justification
- Confirmation que nbconvert --execute passe (exit code 0)
</output>
