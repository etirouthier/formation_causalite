---
phase: 02-scenario0-petits-nombres
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - formation_causalite.ipynb
autonomous: false
requirements:
  - SC0-03

must_haves:
  truths:
    - "La figure sc0_top10.png montre un bar chart des 10 magasins au panier_moyen_moy le plus élevé, barres colorées par taille, avec au moins 50% de petits magasins dans le top 10"
    - "La figure sc0_loi_grands_nombres.png contient 2 sous-graphes empilés (sharex=True) : KDE des données réelles (rang 1) + KDE overlay pour N=10,100,1000,10000,100000 (rang 2)"
    - "Les simulations Figure 4 utilisent rng_fig4 = np.random.default_rng(SEED + 4) local — le rng global n'est pas consommé"
    - "savefig précède plt.show() dans toutes les nouvelles cellules"
    - "Le formateur a validé visuellement les 4 figures du scénario 0"
  artifacts:
    - path: "figures/sc0_top10.png"
      provides: "Figure 3 — top 10 magasins par panier_moyen, barres colorées par taille"
    - path: "figures/sc0_loi_grands_nombres.png"
      provides: "Figure 4 — KDE réel (rang 1) + KDE simulées N=10..100000 (rang 2)"
    - path: "formation_causalite.ipynb"
      provides: "Cellules code-sc0-fig3, code-sc0-fig4 insérées"
      contains: "sc0_top10.png, sc0_loi_grands_nombres.png, rng_fig4"
  key_links:
    - from: "agg_sc0 (plan 02-01)"
      to: "figures/sc0_top10.png"
      via: "agg_sc0.nlargest(10, 'panier_moyen_moy')"
      pattern: "nlargest.*panier_moyen_moy"
    - from: "generate_base_panel + compute_outcomes"
      to: "figures/sc0_loi_grands_nombres.png"
      via: "rng_fig4 local, params_sim avec n_petit=n_moyen=n_grand=N"
      pattern: "rng_fig4"
    - from: "SEED (Paramètres)"
      to: "rng_fig4"
      via: "np.random.default_rng(SEED + 4) — isolé du rng global"
      pattern: "default_rng.*SEED.*4"
---

<objective>
Insérer les Figures 3 et 4 dans le notebook (top 10 magasins et mécanisme loi des grands nombres) puis obtenir la validation visuelle humaine des 4 figures du scénario 0.

Purpose: Figure 3 révèle la sur-représentation des petits magasins dans le top 10 — message pédagogique central du scénario. Figure 4 illustre le mécanisme sous-jacent (la variance diminue à mesure que N augmente) via des simulations DGP.

Output: 2 nouvelles cellules code (code-sc0-fig3, code-sc0-fig4), exports figures/sc0_top10.png et figures/sc0_loi_grands_nombres.png, checkpoint de validation visuelle.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-scenario0-petits-nombres/02-CONTEXT.md
@.planning/phases/02-scenario0-petits-nombres/02-01-SUMMARY.md

**Contrainte absolue :** Livrable = cellules .ipynb. Les 2 nouvelles cellules s'insèrent après la cellule `code-sc0-fig2` (créée dans le plan 02-01).

**Décisions locked (02-CONTEXT.md) :**
- Figure 3 : bar chart des 10 magasins avec `panier_moyen_moy` le plus élevé — barres colorées par taille — critère "analyste naïf" (classement décroissant par moyenne)
- Figure 4 : subplot 2 rangs avec axe X partagé — rang 1 : KDE données réelles de `base_df` — rang 2 : KDE overlay N = 10, 100, 1000, 10000, 100000 via DGP existant
- Simulation Figure 4 : `params_sim = {**PARAMS, 'n_petit': N, 'n_moyen': N, 'n_grand': N}` via `generate_base_panel` + `compute_outcomes(pub=0)`

**Anti-patterns à éviter absolument :**
- NE PAS utiliser le `rng` global dans Figure 4 — créer `rng_fig4 = np.random.default_rng(SEED + 4)` local (sinon décale l'état du rng pour les scénarios 1-3)
- NE PAS recalculer `base_df` — disponible en mémoire depuis Phase 1
- NE PAS ignorer `warn_singular=False` dans `sns.kdeplot` pour N=100000 (std ≈ 0.018, risque de courbe absente)

**Patterns établis en Phase 1 (à respecter) :**
- `fig.savefig('figures/xxx.png', dpi=150, bbox_inches='tight')` AVANT `plt.show()`
- `colors_sc0` défini en `code-sc0-fig1` — disponible en mémoire, ne pas redéfinir

<interfaces>
<!-- Contrats établis dans plan 02-01 — disponibles en mémoire lors de l'exécution -->

**agg_sc0 (200 lignes, plan 02-01) :**
```
Colonnes : magasin_id, taille, n_potentiel, nb_visites_moy, panier_moyen_moy, panier_moyen_std
Résultat empirique SEED=42 :
  - petit=79, moyen=86, grand=35
  - top 10 par panier_moyen_moy : 100% petits magasins (tous les 10)
  - rang 1 : magasin_id≈170, panier_moyen_moy≈53.1€
```

**colors_sc0 (défini en code-sc0-fig1) :**
```python
colors_sc0 = {'petit': '#e74c3c', 'moyen': '#3498db', 'grand': '#2ecc71'}
tailles_ordre = ['petit', 'moyen', 'grand']
```

**DGP pour simulations Figure 4 :**
```python
# Pattern vérifié — résultats empiriques SEED=42
# N=10    : std panier_moyen_moy ≈ 2.01
# N=100   : std ≈ 0.60
# N=1000  : std ≈ 0.19
# N=10000 : std ≈ 0.06
# N=100000: std ≈ 0.018 (quasi-delta — warn_singular=False obligatoire)
params_sim = {**PARAMS, 'n_petit': N, 'n_moyen': N, 'n_grand': N}
sim_df = generate_base_panel(params_sim, rng_fig4)
sim_df['pub'] = 0
sim_df = compute_outcomes(sim_df, params_sim, rng_fig4)
agg_sim = sim_df.groupby('magasin_id')['panier_moyen'].mean()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insérer Figure 3 (top 10 bar chart) et Figure 4 (loi des grands nombres)</name>
  <files>formation_causalite.ipynb</files>
  <action>
Charger formation_causalite.ipynb (JSON). Trouver la cellule `code-sc0-fig2` et insérer deux nouvelles cellules code APRÈS :

**Cellule E — Code (Figure 3 : top 10 bar chart) :**
```python
# ─────────────────────────────────────────────
# Figure 3 — Top 10 magasins par panier moyen (analyse naïve)
# ─────────────────────────────────────────────
top10 = agg_sc0.nlargest(10, 'panier_moyen_moy').reset_index(drop=True)
bar_colors = [colors_sc0[t] for t in top10['taille']]

fig, ax = plt.subplots(figsize=(10, 5))
ax.bar(range(len(top10)), top10['panier_moyen_moy'], color=bar_colors)
ax.set_xticks(range(len(top10)))
ax.set_xticklabels([f"#{i+1}" for i in range(len(top10))], fontsize=10)
ax.set_ylabel('panier_moyen moyen (€)')
ax.set_title('Top 10 magasins par panier moyen — analyse naïve\n'
             "(ce qu'un analyste sans méfiance ferait)")

from matplotlib.patches import Patch
ax.legend(
    handles=[Patch(facecolor=colors_sc0[t], label=t) for t in tailles_ordre],
    title='Taille'
)
fig.savefig('figures/sc0_top10.png', dpi=150, bbox_inches='tight')
plt.show()

n_petits_top10 = (top10['taille'] == 'petit').sum()
print(f"Top 10 : {n_petits_top10}/10 petits magasins ({100*n_petits_top10//10}%)")
```
Attribuer l'id `code-sc0-fig3`.

**Cellule F — Code (Figure 4 : loi des grands nombres) :**
```python
# ─────────────────────────────────────────────
# Figure 4 — Mécanisme : loi des grands nombres
# ─────────────────────────────────────────────
# CRITIQUE : rng_fig4 LOCAL — ne jamais utiliser le rng global ici
# (cela décalerait l'état du générateur pour les scénarios 1-3)
rng_fig4 = np.random.default_rng(SEED + 4)

fig, axes = plt.subplots(2, 1, figsize=(10, 8), sharex=True)

# Rang 1 (haut) : KDE de la distribution réelle (mélange petit/moyen/grand)
sns.kdeplot(data=agg_sc0['panier_moyen_moy'], ax=axes[0],
            fill=True, color='gray', label='Données réelles')
axes[0].set_title('Distribution réelle — mélange de tailles de magasins', fontsize=11)
axes[0].set_ylabel('Densité')
axes[0].set_xlabel('')

# Rang 2 (bas) : KDE overlay pour N = 10, 100, 1000, 10000, 100000
N_values = [10, 100, 1000, 10000, 100000]
palette_fig4 = sns.color_palette('viridis', len(N_values))

for N, color in zip(N_values, palette_fig4):
    params_sim = {**PARAMS, 'n_petit': N, 'n_moyen': N, 'n_grand': N}
    sim_df = generate_base_panel(params_sim, rng_fig4)
    sim_df['pub'] = 0
    sim_df = compute_outcomes(sim_df, params_sim, rng_fig4)
    agg_sim = sim_df.groupby('magasin_id')['panier_moyen'].mean()
    sns.kdeplot(data=agg_sim.dropna(), ax=axes[1],
                label=f'N={N:,}', color=color, warn_singular=False)

axes[1].set_title('Distribution simulée par N_potentiel — loi des grands nombres', fontsize=11)
axes[1].set_xlabel('panier_moyen moyen (€)')
axes[1].set_ylabel('Densité')
axes[1].legend(title='N potentiel')

fig.suptitle('Mécanisme : la variance diminue avec le nombre de clients potentiels',
             fontsize=13)
plt.tight_layout()
fig.savefig('figures/sc0_loi_grands_nombres.png', dpi=150, bbox_inches='tight')
plt.show()
```
Attribuer l'id `code-sc0-fig4`.

Points critiques :
- `rng_fig4 = np.random.default_rng(SEED + 4)` — NE PAS utiliser `rng` global
- `warn_singular=False` dans sns.kdeplot — obligatoire pour N=100000 (std ≈ 0.018)
- `sim_df['pub'] = 0` AVANT `compute_outcomes` — pub doit exister dans le df
- `agg_sim.dropna()` — nettoie les NaN éventuels (défensif)
- `colors_sc0` et `tailles_ordre` sont déjà en mémoire depuis code-sc0-fig1 — ne pas redéfinir
- `SEED` est disponible depuis la cellule Paramètres — pas de magic number
  </action>
  <verify>
    <automated>python3 -c "
import json
nb = json.load(open('formation_causalite.ipynb'))
cells = nb['cells']
ids = [c.get('id','') for c in cells]
assert 'code-sc0-fig3' in ids, 'code-sc0-fig3 manquant'
assert 'code-sc0-fig4' in ids, 'code-sc0-fig4 manquant'
all_code = ' '.join(''.join(c['source']) for c in cells if c['cell_type']=='code')
assert 'nlargest' in all_code, 'nlargest manquant (top10)'
assert 'sc0_top10.png' in all_code, 'export sc0_top10.png manquant'
assert 'sc0_loi_grands_nombres.png' in all_code, 'export sc0_loi_grands_nombres.png manquant'
assert 'rng_fig4' in all_code, 'rng_fig4 manquant — risque de pollution rng global'
assert 'SEED + 4' in all_code or 'SEED+4' in all_code, 'SEED+4 manquant dans rng_fig4'
assert 'warn_singular=False' in all_code, 'warn_singular=False manquant — risque courbe N=100000 absente'
# Vérifier ordre savefig avant show dans chaque cellule
for c in cells:
    if c['cell_type'] == 'code':
        src = ''.join(c['source'])
        if 'savefig' in src and 'plt.show' in src:
            assert src.index('savefig') < src.index('plt.show'), \
                f'savefig APRES plt.show dans cellule {c.get(\"id\",\"?\")} — PNG blanc'
# Vérifier que rng global n'est pas appelé dans code-sc0-fig4
fig4_cell = next(c for c in cells if c.get('id','') == 'code-sc0-fig4')
fig4_src = ''.join(fig4_cell['source'])
# rng_fig4 est ok, mais 'rng,' ou 'rng)' seuls (rng global) seraient un problème
import re
rng_global_calls = re.findall(r'\brng\b(?!_fig4)', fig4_src)
assert not rng_global_calls, f'rng global utilise dans code-sc0-fig4: {rng_global_calls[:3]}'
print(f'OK — {len(cells)} cellules, figures 3 et 4 presentes, rng_fig4 isole')
"</automated>
  </verify>
  <done>
code-sc0-fig3 produit figures/sc0_top10.png (bar chart top 10, barres colorées par taille). code-sc0-fig4 produit figures/sc0_loi_grands_nombres.png (2 sous-graphes sharex, KDE réel + KDE simulées N=10..100000). rng_fig4 local utilisé — rng global non consommé. warn_singular=False présent.
  </done>
</task>

<task type="auto">
  <name>Task 2: Exécuter le notebook et vérifier les 4 exports</name>
  <files>formation_causalite.ipynb</files>
  <action>
Exécuter le notebook complet via nbconvert pour vérifier que l'ensemble Phase 1 + Phase 2 passe sans erreur, et que les 4 figures du scénario 0 sont bien exportées en PNG.

```bash
# Exécution complète
jupyter nbconvert --to notebook --execute formation_causalite.ipynb \
  --output /tmp/test_phase2_complete.ipynb \
  --ExecutePreprocessor.timeout=120

echo "Exit code: $?"

# Vérifier les exports Phase 2
ls -la figures/sc0_distribution.png figures/sc0_scatter.png \
        figures/sc0_top10.png figures/sc0_loi_grands_nombres.png

# Vérifier le CSV
python3 -c "
import pandas as pd
df = pd.read_csv('data/sc0_biais_petits_nombres.csv')
print(f'CSV : {len(df)} magasins, colonnes : {list(df.columns)}')
top10 = df.nlargest(10, 'panier_moyen_moy')
n_petits = (top10['taille'] == 'petit').sum()
print(f'Top 10 : {n_petits}/10 petits magasins')
"
```

Si nbconvert retourne un exit code non-zero, lire la sortie d'erreur et corriger le code des cellules sc0 dans le notebook avant de passer au checkpoint.
  </action>
  <verify>
    <automated>jupyter nbconvert --to notebook --execute formation_causalite.ipynb --output /tmp/test_phase2_final.ipynb --ExecutePreprocessor.timeout=120 2>&1; echo "EXIT:$?"</automated>
  </verify>
  <done>
nbconvert --execute retourne exit code 0. Les 4 fichiers PNG existent dans figures/ (sc0_distribution.png, sc0_scatter.png, sc0_top10.png, sc0_loi_grands_nombres.png). data/sc0_biais_petits_nombres.csv contient 200 lignes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Scénario 0 complet — 4 figures pédagogiques et 1 CSV exportés :
- figures/sc0_distribution.png : 3 histogrammes empilés (petit / moyen / grand), axe X partagé
- figures/sc0_scatter.png : scatter 200 points, corrélation négative nb_visites ↔ variance panier_moyen
- figures/sc0_top10.png : bar chart top 10 magasins par panier moyen, barres colorées par taille
- figures/sc0_loi_grands_nombres.png : subplot 2 rangs, KDE réel (rang 1) + KDE simulées N=10..100000 (rang 2)
- data/sc0_biais_petits_nombres.csv : 200 lignes, 6 colonnes
  </what-built>
  <how-to-verify>
Vérification en ligne de commande (recommandée) :

```bash
# 1. Exécution complète sans erreur
jupyter nbconvert --to notebook --execute formation_causalite.ipynb \
  --output /tmp/sc0_verify.ipynb --ExecutePreprocessor.timeout=120
echo "Exit code: $?"

# 2. Toutes les figures existent et sont des PNG valides (pas blancs)
file figures/sc0_distribution.png figures/sc0_scatter.png \
     figures/sc0_top10.png figures/sc0_loi_grands_nombres.png

# 3. CSV exporté
python3 -c "
import pandas as pd
df = pd.read_csv('data/sc0_biais_petits_nombres.csv')
top10 = df.nlargest(10, 'panier_moyen_moy')
n_petits = (top10['taille'] == 'petit').sum()
print(f'CSV OK : {len(df)} magasins')
print(f'Top 10 : {n_petits}/10 petits magasins (message pedagogique)')
assert n_petits >= 5, f'Moins de 50% de petits magasins dans le top 10 : {n_petits}/10'
print('Assertion top10 OK')
"
```

Points de contrôle visuels (ouvrir les PNG) :
1. sc0_distribution.png : 3 panneaux empilés bien distincts, axe X partagé visible (même plage), la distribution "petit" est plus large/étalée que "grand"
2. sc0_scatter.png : nuage avec tendance négative claire — les points rouges (petit) en haut à gauche (peu de visites, forte variance), les points verts (grand) en bas à droite
3. sc0_top10.png : barres majoritairement rouges (petits), titre avec mention "analyste naïf", légende présente
4. sc0_loi_grands_nombres.png : rang 1 (KDE réelle, grise), rang 2 (5 courbes KDE, la plus large pour N=10, la plus étroite pour N=100000), légende avec N potentiel
  </how-to-verify>
  <resume-signal>Taper "approved" si les 4 figures sont pédagogiquement lisibles et le notebook s'exécute sans erreur. Sinon décrire les problèmes visuels ou erreurs observés.</resume-signal>
</task>

</tasks>

<verification>
Critères objectifs de complétion du plan 02-02 :

```bash
# 1. Toutes les cellules sc0 présentes
python3 -c "
import json
nb = json.load(open('formation_causalite.ipynb'))
ids = [c.get('id','') for c in nb['cells']]
expected = ['md-sc0-section','code-sc0-aggregation','code-sc0-fig1','code-sc0-fig2','code-sc0-fig3','code-sc0-fig4']
for cid in expected:
    assert cid in ids, f'{cid} manquant'
print(f'OK — {len(nb[\"cells\"])} cellules, toutes cellules sc0 presentes')
"

# 2. Exécution notebook sans erreur (exit code 0)
jupyter nbconvert --to notebook --execute formation_causalite.ipynb \
  --output /tmp/phase2_verify.ipynb --ExecutePreprocessor.timeout=120
echo "Exit: $?"

# 3. Les 5 exports créés
ls -la data/sc0_biais_petits_nombres.csv \
       figures/sc0_distribution.png figures/sc0_scatter.png \
       figures/sc0_top10.png figures/sc0_loi_grands_nombres.png

# 4. Message pédagogique validé
python3 -c "
import pandas as pd
df = pd.read_csv('data/sc0_biais_petits_nombres.csv')
top10 = df.nlargest(10, 'panier_moyen_moy')
n_petits = (top10['taille'] == 'petit').sum()
assert n_petits >= 5, f'Top 10 : seulement {n_petits}/10 petits (< 50%)'
print(f'Message pedagogique OK : {n_petits}/10 petits magasins dans le top 10')
"

# 5. rng global non consommé par Figure 4 (reproductibilité scénarios suivants)
python3 -c "
import json
nb = json.load(open('formation_causalite.ipynb'))
fig4 = next(c for c in nb['cells'] if c.get('id','') == 'code-sc0-fig4')
src = ''.join(fig4['source'])
assert 'rng_fig4' in src, 'rng_fig4 absent'
assert 'warn_singular=False' in src, 'warn_singular absent'
print('OK — rng_fig4 isole, warn_singular present')
"
```
</verification>

<success_criteria>
1. Le notebook s'exécute sans erreur de bout en bout via `jupyter nbconvert --execute` (exit code 0)
2. figures/sc0_top10.png : bar chart avec au moins 50% de petits magasins dans le top 10
3. figures/sc0_loi_grands_nombres.png : 2 sous-graphes, KDE réelle (rang 1) + 5 courbes KDE simulées dont N=100000 visible (rang 2)
4. Le `rng` global n'est pas utilisé dans code-sc0-fig4 — reproductibilité des scénarios suivants garantie
5. Checkpoint humain : le formateur confirme que les 4 figures sont pédagogiquement lisibles
</success_criteria>

<output>
Après complétion, créer `.planning/phases/02-scenario0-petits-nombres/02-02-SUMMARY.md` avec :
- Nombre total de cellules dans le notebook final
- Liste des 6 IDs de cellules sc0 insérées (plans 02-01 + 02-02)
- Résultat empirique de top10 (nombre de petits magasins)
- Confirmation nbconvert --execute exit code 0
- Toute déviation par rapport au plan et justification
- Statut du checkpoint humain
</output>
