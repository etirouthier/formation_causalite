---
phase: 01-fondations
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - formation_causalite.ipynb
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-04

must_haves:
  truths:
    - "Le notebook formation_causalite.ipynb existe et s'ouvre sans erreur"
    - "Toute modification des paramètres numériques se fait uniquement dans la cellule Paramètres (ALL_CAPS)"
    - "Une seule exécution de la cellule Imports crée les dossiers figures/ et data/ s'ils n'existent pas"
    - "L'objet rng est créé via np.random.default_rng(SEED) et passé aux fonctions — aucun np.random.seed() global"
  artifacts:
    - path: "formation_causalite.ipynb"
      provides: "Notebook Jupyter avec cellule Paramètres, cellule Imports/setup, rng initialisé"
      contains: "SEED, N_MAGASINS, T_MOIS, EFFET_PUB_VISITES, PARAMS dict, np.random.default_rng"
  key_links:
    - from: "Cellule Paramètres (PARAMS dict)"
      to: "Toutes les cellules de génération"
      via: "Référence PARAMS['clé'] — jamais de magic number"
      pattern: "PARAMS\\["
    - from: "Cellule Imports"
      to: "figures/, data/"
      via: "Path(...).mkdir(parents=True, exist_ok=True)"
      pattern: "mkdir"
---

<objective>
Créer le notebook formation_causalite.ipynb avec sa structure de base : cellule Paramètres (ALL_CAPS), cellule Imports + RNG + création des dossiers d'export.

Purpose: Ce notebook est le livrable unique du projet. Toutes les phases suivantes ajoutent des cellules à ce fichier. Cette première plan établit les conventions non-négociables (ALL_CAPS, rng explicite, pathlib) que chaque cellule ultérieure devra respecter.

Output: formation_causalite.ipynb avec 3 cellules initiales — titre/intro (markdown), Paramètres (code), Imports+setup (code).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fondations/01-RESEARCH.md

**Contrainte absolue :** Le livrable est un fichier `.ipynb`, pas des scripts Python. Toutes les tâches produisent des cellules de notebook.

**Décisions locked (STATE.md) :**
- Paramètres dans une cellule unique ALL_CAPS — formateur édite un seul endroit
- `rng = np.random.default_rng(SEED)` comme seule source de hasard, passé en argument

<interfaces>
<!-- Patterns obligatoires extraits de la recherche Phase 1 -->

Structure de cellules cibles du notebook :
```
[Cellule 1 — Markdown] Titre + description du notebook
[Cellule 2 — Code]     PARAMÈTRES (ALL_CAPS + PARAMS dict)
[Cellule 3 — Code]     Imports + rng + mkdir figures/ + mkdir data/
```

Template cellule Paramètres (extrait de RESEARCH.md) :
```python
# ═══════════════════════════════════════════════════════════
# PARAMÈTRES — modifiez ici uniquement
# ═══════════════════════════════════════════════════════════

SEED        = 42
N_MAGASINS  = 200
T_MOIS      = 24

N_PETIT  = 30
N_MOYEN  = 150
N_GRAND  = 500

P_BASE_VISITE  = 0.05
EFFET_URBAIN   = 0.03
EFFET_EQUIPE   = 0.02
EFFET_SAISON   = {1: -0.01, 2: -0.01, 3: 0.0, 4: 0.01, 5: 0.02, 6: 0.02,
                   7: 0.02, 8: 0.01, 9: 0.0, 10: -0.01, 11: 0.01, 12: 0.02}

EFFET_PUB_VISITES = 0.10
EFFET_PUB_PANIER  = 0.10

MU_PANIER_BASE = 50.0
SIGMA_PANIER   = 15.0

P_PUB_BONNE_EQUIPE    = 0.70
P_PUB_MAUVAISE_EQUIPE = 0.30
P_PUB_URBAIN          = 0.65
P_PUB_RURAL           = 0.25
P_PUB_HAUTE_SAISON    = 0.70
P_PUB_BASSE_SAISON    = 0.30

PARAMS = {
    'seed': SEED,
    'n_magasins': N_MAGASINS,
    't_mois': T_MOIS,
    'n_petit': N_PETIT,
    'n_moyen': N_MOYEN,
    'n_grand': N_GRAND,
    'p_base_visite': P_BASE_VISITE,
    'effet_urbain': EFFET_URBAIN,
    'effet_equipe': EFFET_EQUIPE,
    'effet_saison': EFFET_SAISON,
    'effet_pub_visites': EFFET_PUB_VISITES,
    'effet_pub_panier': EFFET_PUB_PANIER,
    'mu_panier_base': MU_PANIER_BASE,
    'sigma_panier': SIGMA_PANIER,
    'p_pub_bonne_equipe': P_PUB_BONNE_EQUIPE,
    'p_pub_mauvaise_equipe': P_PUB_MAUVAISE_EQUIPE,
    'p_pub_urbain': P_PUB_URBAIN,
    'p_pub_rural': P_PUB_RURAL,
    'p_pub_haute_saison': P_PUB_HAUTE_SAISON,
    'p_pub_basse_saison': P_PUB_BASSE_SAISON,
}
```

Template cellule Imports (extrait de RESEARCH.md) :
```python
import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
import matplotlib
import networkx as nx
import statsmodels.formula.api as smf
import seaborn as sns
from pathlib import Path

matplotlib.rcParams['figure.dpi'] = 100

rng = np.random.default_rng(SEED)

Path('figures').mkdir(parents=True, exist_ok=True)
Path('data').mkdir(parents=True, exist_ok=True)
print("Dossiers figures/ et data/ prêts.")
print(f"RNG initialisé avec SEED={SEED}")
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Créer le notebook formation_causalite.ipynb avec cellule Paramètres</name>
  <files>formation_causalite.ipynb</files>
  <action>
Créer le fichier formation_causalite.ipynb au format JSON Jupyter Notebook (nbformat 4.4). Le notebook doit contenir exactement 2 cellules dans cet ordre :

**Cellule 1 — Markdown (titre) :**
```markdown
# Générateur de données — Formation Causalité

Notebook de génération de données simulées illustrant quatre biais causaux classiques.
Tous les paramètres de simulation se trouvent dans la cellule **PARAMÈTRES** ci-dessous.

**Usage :** Kernel → Restart & Run All — aucune modification nécessaire hors de la cellule Paramètres.

---
Scénarios couverts :
- **Scénario 0** — Biais de petits nombres (variance binomiale)
- **Scénarios 1a/1b/1c** — Biais de sélection (confondant)
- **Scénario 2** — Surcontrôle sur un médiateur
- **Scénario 3** — Surcontrôle sur un collider
```

**Cellule 2 — Code (Paramètres) :**
Contient exactement le template de la cellule Paramètres fourni dans la section `<interfaces>` ci-dessus, avec les valeurs par défaut :
- SEED=42, N_MAGASINS=200, T_MOIS=24
- N_PETIT=30, N_MOYEN=150, N_GRAND=500
- P_BASE_VISITE=0.05, EFFET_URBAIN=0.03, EFFET_EQUIPE=0.02
- EFFET_SAISON dict {1: -0.01, ..., 12: 0.02} (valeurs exactes dans le template)
- EFFET_PUB_VISITES=0.10, EFFET_PUB_PANIER=0.10
- MU_PANIER_BASE=50.0, SIGMA_PANIER=15.0
- P_PUB_BONNE_EQUIPE=0.70, P_PUB_MAUVAISE_EQUIPE=0.30
- P_PUB_URBAIN=0.65, P_PUB_RURAL=0.25
- P_PUB_HAUTE_SAISON=0.70, P_PUB_BASSE_SAISON=0.30
- PARAMS dict regroupant toutes les constantes

Le fichier .ipynb est un JSON valide avec `"nbformat": 4, "nbformat_minor": 4`.
Chaque cellule code a `"cell_type": "code"`, `"metadata": {}`, `"outputs": []`, `"execution_count": null`.
Chaque cellule markdown a `"cell_type": "markdown"`, `"metadata": {}`.

NE PAS inclure de magic numbers dans des cellules autres que la cellule Paramètres.
  </action>
  <verify>
    <automated>python -c "import json; nb=json.load(open('formation_causalite.ipynb')); assert nb['nbformat']==4; cells=nb['cells']; assert cells[0]['cell_type']=='markdown'; assert cells[1]['cell_type']=='code'; src=''.join(cells[1]['source']); assert 'SEED' in src and 'N_MAGASINS' in src and 'PARAMS' in src and 'EFFET_PUB_VISITES' in src; print('OK — cellule Parametres valide')"</automated>
  </verify>
  <done>formation_causalite.ipynb existe, est un JSON nbformat 4 valide, contient une cellule markdown titre et une cellule code Paramètres avec toutes les constantes ALL_CAPS et le dict PARAMS.</done>
</task>

<task type="auto">
  <name>Task 2: Ajouter la cellule Imports + RNG + setup dossiers</name>
  <files>formation_causalite.ipynb</files>
  <action>
Ajouter une troisième cellule code au notebook (après la cellule Paramètres) contenant :

```python
# ═══════════════════════════════════════════════════════════
# IMPORTS ET CONFIGURATION
# ═══════════════════════════════════════════════════════════

import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
import matplotlib
import networkx as nx
import statsmodels.formula.api as smf
import seaborn as sns
from pathlib import Path

# Configuration matplotlib
matplotlib.rcParams['figure.dpi'] = 100
matplotlib.rcParams['font.size'] = 11

# RNG reproductible — seule source de hasard du notebook
# Passer rng en argument à chaque fonction qui en a besoin
# NE PAS utiliser np.random.seed() ni np.random.binomial() directement
rng = np.random.default_rng(SEED)

# Création automatique des dossiers d'export
Path('figures').mkdir(parents=True, exist_ok=True)
Path('data').mkdir(parents=True, exist_ok=True)

print(f"RNG initialisé avec SEED={SEED}")
print("Dossiers figures/ et data/ prets.")
```

Modifier le fichier .ipynb en chargeant son contenu JSON, ajoutant la cellule, et réécrivant le fichier. La cellule doit être insérée à l'index 2 (troisième cellule, après titre et Paramètres).

CRITIQUE : `rng = np.random.default_rng(SEED)` doit référencer la constante `SEED` définie dans la cellule Paramètres — pas un literal entier.

CRITIQUE : Ne jamais écrire `np.random.seed()` ou utiliser `np.random` directement pour les tirages aléatoires.
  </action>
  <verify>
    <automated>python -c "
import json
nb = json.load(open('formation_causalite.ipynb'))
cells = nb['cells']
assert len(cells) == 3, f'Expected 3 cells, got {len(cells)}'
src = ''.join(cells[2]['source'])
assert 'np.random.default_rng(SEED)' in src, 'rng init manquant'
assert 'Path' in src and 'mkdir' in src, 'mkdir manquant'
assert 'figures' in src and 'data' in src, 'dossiers manquants'
assert 'np.random.seed' not in src, 'legacy np.random.seed() trouve - interdit'
print('OK — cellule Imports valide')
"</automated>
  </verify>
  <done>Le notebook a 3 cellules. La cellule Imports initialise rng via np.random.default_rng(SEED), importe tous les packages nécessaires, et crée figures/ et data/ via pathlib. Aucun np.random.seed() présent.</done>
</task>

</tasks>

<verification>
Vérification globale après les deux tâches :

```bash
python -c "
import json
nb = json.load(open('formation_causalite.ipynb'))
assert nb['nbformat'] == 4
cells = nb['cells']
assert len(cells) == 3
# Cellule 1 : titre markdown
assert cells[0]['cell_type'] == 'markdown'
# Cellule 2 : Paramètres ALL_CAPS
params_src = ''.join(cells[1]['source'])
for const in ['SEED', 'N_MAGASINS', 'T_MOIS', 'N_PETIT', 'N_GRAND', 'EFFET_PUB_VISITES', 'EFFET_PUB_PANIER', 'PARAMS']:
    assert const in params_src, f'{const} manquant dans cellule Parametres'
# Cellule 3 : Imports + rng + mkdir
imports_src = ''.join(cells[2]['source'])
assert 'np.random.default_rng(SEED)' in imports_src
assert 'Path' in imports_src and 'mkdir' in imports_src
assert 'np.random.seed' not in imports_src
print('PASS — notebook skeleton valide')
"
```

Puis test d'exécution réel via nbconvert (vérifie que les cellules s'exécutent sans erreur avec les dépendances installées) :
```bash
jupyter nbconvert --to notebook --execute formation_causalite.ipynb --output formation_causalite_test.ipynb 2>&1 | tail -5
```
Si nbconvert échoue sur un import manquant, installer les dépendances : `pip install "numpy>=2.0" "pandas>=2.2" "matplotlib>=3.8" "seaborn>=0.13" "statsmodels>=0.14" "networkx>=3.0"`.
</verification>

<success_criteria>
1. formation_causalite.ipynb est un JSON nbformat 4 valide avec 3 cellules
2. Tous les paramètres numériques (SEED, N_MAGASINS, N_PETIT, N_GRAND, EFFET_PUB_VISITES, etc.) sont dans la cellule Paramètres sous forme de constantes ALL_CAPS
3. Le dict PARAMS regroupe toutes les constantes pour passage aux fonctions
4. rng est initialisé via np.random.default_rng(SEED) — aucun np.random.seed() global
5. figures/ et data/ sont créés via Path(...).mkdir(parents=True, exist_ok=True)
6. Les 3 cellules s'exécutent sans erreur avec `jupyter nbconvert --execute`
</success_criteria>

<output>
Après complétion, créer `.planning/phases/01-fondations/01-01-SUMMARY.md` avec :
- Fichiers créés/modifiés
- Patterns établis (ALL_CAPS, rng, pathlib)
- Toute déviation par rapport au plan et pourquoi
- État du notebook (nombre de cellules, constantes définies)
</output>
