---
phase: 05-scenario3-collider
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - formation_causalite.ipynb
autonomous: true
requirements: [SC3-01, SC3-02, SC3-03]

must_haves:
  truths:
    - "Le scénario 3 affiche un DAG 3 noeuds en V inversé (Pub et Ventes pointent vers Posts réseaux — collider), exporté figures/sc3_dag.png"
    - "Le coefficient plot montre OLS naïf (~33.6%) > OLS avec posts_reseaux (~30.5%) — biais vers le bas d'au moins 5% relatif visible"
    - "Le bar chart compare 3 estimateurs : OLS sans collider (rouge, correct), OLS avec posts_reseaux (orange, biaisé), Valeur vraie ATT (vert)"
    - "posts_reseaux est calculé APRÈS compute_outcomes (nécessite ventes observées avec effet pub) et fusionné au niveau magasin dans df_sc3"
    - "Seed rng_sc3 = np.random.default_rng(SEED + 50), seed contrefactuel rng_cf_sc3 = np.random.default_rng(SEED + 51), isolés"
    - "COLLIDER_PUB_VENTES_COEFF = 5e-5 ajouté dans la cellule Paramètres (cell 1) ET dans le dict PARAMS"
    - "CSV exporté data/sc3_collider.csv avec colonne posts_reseaux"
    - "Le notebook s'exécute de bout en bout (nbconvert exit 0) avec 41 cellules au total"
  artifacts:
    - path: "figures/sc3_dag.png"
      provides: "DAG Scénario 3 — V-structure collider"
    - path: "figures/sc3_coeff.png"
      provides: "Coefficient plot Scénario 3"
    - path: "figures/sc3_bar.png"
      provides: "Bar chart Scénario 3"
    - path: "data/sc3_collider.csv"
      provides: "Dataset scénario 3 avec posts_reseaux"
  key_links:
    - from: "code-sc3-data"
      to: "posts_reseaux"
      via: "calculé après compute_outcomes depuis ventes_agg de df_sc3 (pas base_df)"
      pattern: "df_sc3\\.groupby\\('magasin_id'\\)\\['ventes'\\]\\.sum"
    - from: "code-sc3-data"
      to: "model_naive_sc3"
      via: "OLS log_rev_int ~ pub (sans collider = effet total correct)"
      pattern: "log_rev_int ~ pub'"
    - from: "code-sc3-data"
      to: "model_coll_sc3"
      via: "OLS log_rev_int ~ pub + posts_reseaux (collider crée chemin non causal = biaisé)"
      pattern: "log_rev_int ~ pub \\+ posts_reseaux"
    - from: "code-sc3-dag"
      to: "G_sc3"
      via: "V-structure : Pub→Posts réseaux et Ventes→Posts réseaux (les deux causent le collider)"
      pattern: "Ventes.*Posts réseaux"
---

<objective>
Insérer le scénario 3 (surcontrôle sur un collider) dans le notebook : modifier la cellule Paramètres, insérer 5 cellules (1 markdown + 4 code), exécuter le notebook complet, exporter 3 PNG et 1 CSV.

Purpose: Illustrer que contrôler sur `posts_reseaux` (un collider causé par pub ET ventes) biaise le coefficient de `pub` alors que l'estimation naïve était correcte. Contraste pédagogique : Phase 3 (confondant → ajuster corrige), Phase 4 (médiateur → ajuster biaise), Phase 5 (collider → ajuster biaise aussi, mécanisme différent).

Output: 3 PNG (sc3_dag.png, sc3_coeff.png, sc3_bar.png), 1 CSV (sc3_collider.csv), 5 nouvelles cellules dans le notebook (cellules 36-40, soit 41 cellules au total), COLLIDER_PUB_VENTES_COEFF ajouté dans la cellule Paramètres.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-scenario3-collider/05-CONTEXT.md
@.planning/phases/05-scenario3-collider/05-RESEARCH.md

<interfaces>
<!-- Fonctions DGP disponibles dans le notebook (cellules 4-5, déjà définies) -->

```python
# generate_base_panel(params, rng) — retourne base_df (4800 lignes, 200 magasins × 24 mois)
# Colonnes : magasin_id, taille, urbain, qualite_equipe, mois, effet_saison_val,
#            n_potentiel, nb_visites, ventes, panier_moyen (baseline pub=0)

# compute_outcomes(df, params, rng) — recalcule ventes et panier_moyen selon df['pub']
# Fait .copy() en interne — safe à appeler plusieurs fois

# smf = statsmodels.formula.api (importé cellule 2)
# nx = networkx (importé cellule 2)
# base_df = panel partagé disponible en mémoire (cellule 5 déjà exécutée)
# SEED = 42 (cellule 1)
# PARAMS dict disponible (cellule 1)
# Convention seed par phase : SEED + 10×phase_number
```

<!-- Pattern assignation pub aléatoire niveau magasin (réutilisé sc2, adapté sc3) -->

```python
rng_sc3 = np.random.default_rng(SEED + 50)
stores_sc3 = base_df[['magasin_id']].drop_duplicates('magasin_id').copy()
stores_sc3['pub'] = rng_sc3.binomial(1, 0.5, size=len(stores_sc3))
df_sc3 = base_df.merge(stores_sc3[['magasin_id', 'pub']], on='magasin_id')
df_sc3 = compute_outcomes(df_sc3, PARAMS, rng_sc3)
df_sc3['log_rev_int'] = np.log(df_sc3['ventes'] / df_sc3['n_potentiel'])
```

<!-- Collider DGP — calculé APRÈS compute_outcomes, au niveau magasin -->

```python
# CRITICAL: ventes_agg doit venir de df_sc3 (ventes avec effet pub), pas base_df
ventes_agg = df_sc3.groupby('magasin_id')['ventes'].sum().reset_index()
ventes_agg.columns = ['magasin_id', 'ventes_agg']
stores_collider = stores_sc3.merge(ventes_agg, on='magasin_id')
bruit_sc3 = rng_sc3.normal(0, 1, size=len(stores_collider))
stores_collider['posts_reseaux'] = (
    COLLIDER_PUB_VENTES_COEFF * stores_collider['pub'] * stores_collider['ventes_agg']
    + bruit_sc3
)
df_sc3 = df_sc3.merge(stores_collider[['magasin_id', 'posts_reseaux']], on='magasin_id')
# Note: contrôle pub=0 → posts_reseaux ~ N(0,1), contrôle pub=1 → posts_reseaux ~ N(alpha*ventes,1)
```

<!-- Pattern ATT contrefactuel (même que sc2, adapté sc3) -->

```python
rng_cf_sc3 = np.random.default_rng(SEED + 51)  # seed DÉDIÉ, isolé de rng_sc3
df_cf_sc3 = df_treated_sc3.copy()
df_cf_sc3['pub'] = 0
df_cf_sc3 = compute_outcomes(df_cf_sc3, PARAMS, rng_cf_sc3)
df_cf_sc3['log_rev_int'] = np.log(df_cf_sc3['ventes'] / df_cf_sc3['n_potentiel'])
att_sc3_log = (log_Y1_sc3 - log_Y0_sc3).mean()
```

<!-- Résultats empiriques attendus (SEED=42, COLLIDER_PUB_VENTES_COEFF=5e-5) -->

```
ATT_sc3_log          ≈ 30.2%   (valeur vraie contrefactuelle)
OLS naïf (sans coll) ≈ 33.6%   (correct — assignation aléatoire)
OLS sur-contrôlé     ≈ 30.5%   (biaisé vers le bas — collider)
Biais relatif        ≈ 9.4%    (> seuil 5% requis)
Traités              ≈ 113/200  magasins
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Modifier la cellule Paramètres et insérer les 5 cellules du scénario 3</name>
  <files>formation_causalite.ipynb</files>
  <action>
Deux modifications au notebook :

**Partie A — Modifier la cellule Paramètres (cell 0, index 0) :**

Lire le notebook JSON. Dans la cellule d'index 0 (cellule Paramètres ALL_CAPS), ajouter après la ligne `EFFET_PUB_PANIER` (ou à la fin des constantes ALL_CAPS) :

```python
COLLIDER_PUB_VENTES_COEFF = 5e-5   # sc3 : intensité du signal collider pub×ventes
```

Et dans le dict `PARAMS`, ajouter la clé :

```python
'collider_pub_ventes_coeff': COLLIDER_PUB_VENTES_COEFF,
```

**Partie B — Insérer 5 cellules après cell index 35 (dernière cellule = code-sc2-bar) :**

Le notebook passe de 36 à 41 cellules.

**Cellule 36 — Markdown section sc3 (cell_type: markdown) :**
```
---
## Scénario 3 — Surcontrôle sur un collider

`posts_reseaux` est un collider : à la fois `pub → posts_reseaux` ET `ventes → posts_reseaux`. Contrôler sur un collider dans la régression ouvre un chemin non causal entre `pub` et `ventes`, introduisant un biais là où l'estimation naïve était correcte. Contrairement au scénario 2, ce n'est pas le chemin causal qui est bloqué — c'est un chemin parasite qui est ouvert.
```

**Cellule 37 — code-sc3-data (cell_type: code) :**
```python
# ─────────────────────────────────────────────
# Scénario 3 — Surcontrôle sur un collider
# Assignation pub : aléatoire niveau magasin (pas de confondant)
# Collider : posts_reseaux causé par PUB ET VENTES
# ─────────────────────────────────────────────

rng_sc3 = np.random.default_rng(SEED + 50)

# Assignation pub aléatoire au niveau magasin (p=0.5 — pas de confondant)
stores_sc3 = base_df[['magasin_id']].drop_duplicates('magasin_id').copy()
stores_sc3['pub'] = rng_sc3.binomial(1, 0.5, size=len(stores_sc3))

# Panel complet + outcomes observés
df_sc3 = base_df.merge(stores_sc3[['magasin_id', 'pub']], on='magasin_id')
df_sc3 = compute_outcomes(df_sc3, PARAMS, rng_sc3)
df_sc3['log_rev_int'] = np.log(df_sc3['ventes'] / df_sc3['n_potentiel'])

# Collider DGP — APRÈS compute_outcomes (nécessite ventes observées avec effet pub)
# Contrôle pub=0 : posts_reseaux ~ N(0,1) ; Traité pub=1 : posts_reseaux ~ N(alpha*ventes_agg, 1)
ventes_agg = df_sc3.groupby('magasin_id')['ventes'].sum().reset_index()
ventes_agg.columns = ['magasin_id', 'ventes_agg']
stores_collider = stores_sc3.merge(ventes_agg, on='magasin_id')
bruit_sc3 = rng_sc3.normal(0, 1, size=len(stores_collider))
stores_collider['posts_reseaux'] = (
    COLLIDER_PUB_VENTES_COEFF * stores_collider['pub'] * stores_collider['ventes_agg']
    + bruit_sc3
)
df_sc3 = df_sc3.merge(stores_collider[['magasin_id', 'posts_reseaux']], on='magasin_id')

# Calcul ATT contrefactuel (rng dédié pour isolation)
treated_ids_sc3 = stores_sc3[stores_sc3['pub'] == 1]['magasin_id'].values
df_treated_sc3 = df_sc3[df_sc3['magasin_id'].isin(treated_ids_sc3)].copy()
rng_cf_sc3 = np.random.default_rng(SEED + 51)
df_cf_sc3 = df_treated_sc3.copy()
df_cf_sc3['pub'] = 0
df_cf_sc3 = compute_outcomes(df_cf_sc3, PARAMS, rng_cf_sc3)
df_cf_sc3['log_rev_int'] = np.log(df_cf_sc3['ventes'] / df_cf_sc3['n_potentiel'])
log_Y1_sc3 = np.log(df_treated_sc3['ventes'] / df_treated_sc3['n_potentiel'])
log_Y0_sc3 = np.log(df_cf_sc3['ventes'] / df_cf_sc3['n_potentiel'])
att_sc3_log = (log_Y1_sc3 - log_Y0_sc3).mean()

# OLS naïf : effet total (correct car assignation aléatoire, pas de confondant)
model_naive_sc3 = smf.ols('log_rev_int ~ pub', data=df_sc3).fit()
# OLS sur-contrôlé : biaisé (le collider ouvre un chemin non causal)
model_coll_sc3  = smf.ols('log_rev_int ~ pub + posts_reseaux', data=df_sc3).fit()

# Export CSV
df_sc3.to_csv('data/sc3_collider.csv', index=False)
print(f"Sc3 — Traités: {len(treated_ids_sc3)}/200 magasins")
print(f"Sc3 — ATT: {att_sc3_log*100:.1f}%  |  OLS naïf: {model_naive_sc3.params['pub']*100:.1f}%  |  OLS avec posts_reseaux: {model_coll_sc3.params['pub']*100:.1f}%")
```

**Cellule 38 — code-sc3-dag (cell_type: code) :**
```python
# ─────────────────────────────────────────────
# Figure DAG — Scénario 3
# V-structure (collider) : Pub → Posts réseaux ← Ventes
# Pub → Ventes inclus pour montrer la structure causale complète
# ─────────────────────────────────────────────

G_sc3 = nx.DiGraph()
G_sc3.add_edges_from([
    ('Pub', 'Ventes'),           # effet causal de la pub sur les ventes
    ('Pub', 'Posts réseaux'),    # pub → collider
    ('Ventes', 'Posts réseaux'), # ventes → collider (V-structure)
])
pos_sc3 = {
    'Pub':           (0, 1),
    'Ventes':        (0, 0),
    'Posts réseaux': (1, 0.5),
}
color_map_sc3 = {
    'Pub':           'steelblue',
    'Ventes':        'seagreen',
    'Posts réseaux': 'crimson',    # collider = rouge distinctif
}
node_colors_sc3 = [color_map_sc3[n] for n in G_sc3.nodes()]

fig, ax = plt.subplots(figsize=(5, 3.5))
nx.draw_networkx(G_sc3, pos_sc3, ax=ax,
                 node_color=node_colors_sc3,
                 node_size=2000, font_size=9, font_color='white',
                 arrows=True, arrowsize=20)
ax.axis('off')
ax.set_title('DAG — Scénario 3 : collider posts_reseaux (V-structure)')
fig.savefig('figures/sc3_dag.png', dpi=150, bbox_inches='tight')
plt.show()
```

**Cellule 39 — code-sc3-coeff (cell_type: code) :**
```python
# ─────────────────────────────────────────────
# Figure coefficient plot — Scénario 3
# 3 points : OLS naïf (rouge, correct), OLS avec posts_reseaux (orange, biaisé), ATT (vert)
# ─────────────────────────────────────────────

coef_naive_sc3 = model_naive_sc3.params['pub']
ci_naive_sc3   = model_naive_sc3.conf_int()
xerr_naive_sc3 = [[coef_naive_sc3 - ci_naive_sc3.loc['pub', 0]],
                   [ci_naive_sc3.loc['pub', 1] - coef_naive_sc3]]

coef_coll_sc3 = model_coll_sc3.params['pub']
ci_coll_sc3   = model_coll_sc3.conf_int()
xerr_coll_sc3 = [[coef_coll_sc3 - ci_coll_sc3.loc['pub', 0]],
                  [ci_coll_sc3.loc['pub', 1] - coef_coll_sc3]]

estimators_sc3 = ['OLS sans collider', 'OLS avec posts_reseaux', 'Valeur vraie (ATT)']
y_pos_sc3 = [2, 1, 0]

fig, ax = plt.subplots(figsize=(8, 4))
ax.errorbar(coef_naive_sc3, 2, xerr=xerr_naive_sc3, fmt='o', capsize=5,
            color='#e74c3c', label='OLS sans collider', markersize=8)
ax.errorbar(coef_coll_sc3, 1, xerr=xerr_coll_sc3, fmt='o', capsize=5,
            color='#e67e22', label='OLS avec posts_reseaux', markersize=8)
ax.errorbar(att_sc3_log, 0, fmt='D', color='#2ecc71',
            label='Valeur vraie (ATT)', markersize=10)
ax.axvline(x=att_sc3_log, color='gray', linestyle='--', alpha=0.5)
ax.set_yticks(y_pos_sc3)
ax.set_yticklabels(estimators_sc3)
ax.set_xlabel('Uplift log des ventes (≈ %)')
ax.set_title('Scénario 3 — Coefficients OLS sans vs avec collider vs valeur vraie')
ax.legend(loc='lower right')
fig.savefig('figures/sc3_coeff.png', dpi=150, bbox_inches='tight')
plt.show()
```

**Cellule 40 — code-sc3-bar (cell_type: code) :**
```python
# ─────────────────────────────────────────────
# Figure bar chart — Scénario 3
# Sans collider (rouge, correct) vs Avec posts_reseaux (orange, biaisé) vs ATT (vert)
# Proximité ATT ≈ avec-collider est fortuite (coïncidence, pas une correction)
# ─────────────────────────────────────────────

fig, ax = plt.subplots(figsize=(7, 4))
labels_sc3 = ['OLS sans collider', 'OLS avec posts_reseaux', 'Valeur vraie (ATT)']
values_sc3 = [coef_naive_sc3, coef_coll_sc3, att_sc3_log]
colors_sc3 = ['#e74c3c', '#e67e22', '#2ecc71']

ax.bar(range(len(labels_sc3)), values_sc3, color=colors_sc3, width=0.5)
ax.set_xticks(range(len(labels_sc3)))
ax.set_xticklabels(labels_sc3)
ax.set_ylabel('Uplift log des ventes (≈ %)')
ax.set_title('Scénario 3 — Comparaison des estimateurs')
ax.axhline(y=att_sc3_log, color='gray', linestyle='--', alpha=0.5)
fig.savefig('figures/sc3_bar.png', dpi=150, bbox_inches='tight')
plt.show()
```

Après insertion des cellules, exécuter le notebook complet :
```bash
jupyter nbconvert --to notebook --execute formation_causalite.ipynb \
    --output formation_causalite.ipynb \
    --ExecutePreprocessor.timeout=300
```

Anti-patterns CRITIQUES pour sc3 :
- NE PAS calculer `posts_reseaux` depuis `base_df` — utiliser `df_sc3` (APRÈS compute_outcomes) pour que ventes_agg inclue l'effet pub
- `rng_cf_sc3 = np.random.default_rng(SEED + 51)` — seed DÉDIÉ pour le contrefactuel (pas rng_sc3)
- PAS de `C(mois)` dans les formules OLS — assignation aléatoire, pas de confondant saisonnier
- `COLLIDER_PUB_VENTES_COEFF` DOIT être dans la cellule Paramètres (cell 0) ET dans le dict PARAMS
- `node_colors_sc3 = [color_map_sc3[n] for n in G_sc3.nodes()]` — pas une liste littérale
- `fig.savefig(...)` AVANT `plt.show()` dans chaque cellule figure
- `coef_naive_sc3` doit être SUPÉRIEUR à `coef_coll_sc3` (biais vers le bas) — si l'inverse, vérifier DGP
- Fusion `posts_reseaux` dans df_sc3 avant export CSV — le CSV doit contenir la colonne posts_reseaux
  </action>
  <verify>
    <automated>python3 -c "
import json, os

# 1. Vérifier structure des cellules sc3 dans le notebook
with open('formation_causalite.ipynb') as f:
    nb = json.load(f)
sources = [''.join(c['source']) for c in nb['cells']]
all_src = '\n'.join(sources)

checks = {
    'sc3 markdown present': 'Scénario 3' in all_src and 'collider' in all_src,
    'COLLIDER_PUB_VENTES_COEFF in params cell': 'COLLIDER_PUB_VENTES_COEFF' in ''.join(nb['cells'][0]['source']),
    'collider_pub_ventes_coeff in PARAMS dict': 'collider_pub_ventes_coeff' in all_src,
    'rng_sc3 SEED+50': 'np.random.default_rng(SEED + 50)' in all_src,
    'rng_cf_sc3 SEED+51': 'np.random.default_rng(SEED + 51)' in all_src,
    'pub assignment p=0.5': 'binomial(1, 0.5' in all_src,
    'ventes_agg from df_sc3': 'df_sc3.groupby' in all_src,
    'posts_reseaux collider DGP': 'posts_reseaux' in all_src and 'COLLIDER_PUB_VENTES_COEFF' in all_src,
    'log_rev_int sc3': \"df_sc3['log_rev_int']\" in all_src or 'df_sc3[\"log_rev_int\"]' in all_src,
    'OLS naif sans collider': \"'log_rev_int ~ pub'\" in all_src or '\"log_rev_int ~ pub\"' in all_src,
    'OLS avec posts_reseaux': 'log_rev_int ~ pub + posts_reseaux' in all_src,
    'att_sc3_log computed': 'att_sc3_log' in all_src,
    'DAG V-structure 3 nodes': 'G_sc3' in all_src and 'Posts réseaux' in all_src and 'Ventes.*Posts réseaux' in all_src or ('Ventes' in all_src and 'Posts réseaux' in all_src),
    'collider color crimson': 'crimson' in all_src,
    'sc3_dag.png exported': 'sc3_dag.png' in all_src,
    'sc3_coeff.png exported': 'sc3_coeff.png' in all_src,
    'sc3_bar.png exported': 'sc3_bar.png' in all_src,
    'sc3_collider.csv exported': 'sc3_collider.csv' in all_src,
    'xlabel uplift log sc3': 'Uplift log des ventes' in all_src,
    'nb == 41 cells': len(nb['cells']) == 41,
}
for k, v in checks.items():
    print(f'{\"OK\" if v else \"FAIL\"}: {k}')
print(f'Total cells: {len(nb[\"cells\"])}')

# 2. Vérifier les fichiers exportés
files_to_check = [
    'figures/sc3_dag.png',
    'figures/sc3_coeff.png',
    'figures/sc3_bar.png',
    'data/sc3_collider.csv',
]
for p in files_to_check:
    exists = os.path.exists(p)
    checks[p] = exists
    print(f'{\"OK\" if exists else \"FAIL\"}: {p} exists')

# 3. Résultat global
failed = [k for k, v in checks.items() if not v]
if failed:
    print(f'FAIL: {len(failed)} checks failed: {failed}')
    exit(1)
print('PASS: all checks passed')
"
</automated>
  </verify>
  <done>5 cellules scénario 3 insérées (1 markdown + 4 code), COLLIDER_PUB_VENTES_COEFF ajouté en cellule Paramètres. Notebook exécuté (nbconvert exit 0, 41 cellules). 3 PNG (sc3_dag, sc3_coeff, sc3_bar) et 1 CSV (sc3_collider.csv avec posts_reseaux) produits. OLS naïf (~33.6%) > OLS avec posts_reseaux (~30.5%) — biais relatif ≥ 5% visible. Seeds SEED+50 et SEED+51 isolés. DAG V-structure en rouge (crimson) pour le collider.</done>
</task>

</tasks>

<verification>
Vérifications globales du plan 05-01 :

1. `jupyter nbconvert --to notebook --execute formation_causalite.ipynb --output /tmp/test_05_01.ipynb --ExecutePreprocessor.timeout=300` — exit code 0
2. `ls figures/sc3_dag.png figures/sc3_coeff.png figures/sc3_bar.png` — 3 fichiers présents
3. `ls data/sc3_collider.csv` — CSV présent avec colonne posts_reseaux
4. Print dans le notebook : OLS naïf ≈ 33.6% > OLS avec posts_reseaux ≈ 30.5% (biais ≥ 5% relatif)
5. DAG contient 3 noeuds (Pub, Ventes, Posts réseaux) en V-structure, Posts réseaux en crimson
6. Notebook a exactement 41 cellules
7. COLLIDER_PUB_VENTES_COEFF = 5e-5 présent en cellule 0 ET dans PARAMS dict
</verification>

<success_criteria>
- COLLIDER_PUB_VENTES_COEFF = 5e-5 ajouté dans la cellule Paramètres (cell 0) et dans le dict PARAMS
- 5 cellules scénario 3 insérées (1 markdown + 4 code) — notebook passe de 36 à 41 cellules
- 3 PNG exportés dans figures/ : sc3_dag.png, sc3_coeff.png, sc3_bar.png
- 1 CSV exporté dans data/ : sc3_collider.csv (avec colonne posts_reseaux au niveau magasin)
- nbconvert exit 0 sur le notebook complet (41 cellules)
- OLS naïf (≈33.6%) > OLS avec posts_reseaux (≈30.5%) — biais relatif ≥ 5% pédagogiquement visible
- DAG V-structure : 3 noeuds (Pub, Ventes, Posts réseaux en crimson), arêtes Pub→Posts réseaux et Ventes→Posts réseaux
- Seeds isolés : rng_sc3 = SEED+50, rng_cf_sc3 = SEED+51
- posts_reseaux calculé APRÈS compute_outcomes depuis df_sc3 (ventes avec effet pub)
- xlabel/ylabel : "Uplift log des ventes (≈ %)" cohérent avec phases précédentes
</success_criteria>

<output>
After completion, create `.planning/phases/05-scenario3-collider/05-01-SUMMARY.md`
</output>
